---
phase: 03-backend-enforcement-lot-batch-stock-control
plan: 09
type: execute
wave: 5
depends_on: ["04", "05", "07"]
files_modified:
  - apps/backend/tests/salesOrder.license-block.test.ts
  - apps/backend/tests/order.genericUpdateRestriction.test.ts
  - apps/backend/tests/audit.crud.orders.test.ts
  - apps/backend/tests/models.txClient.test.ts
  - apps/backend/tests/rbac.enforcement.writes.orders.test.ts
  - apps/backend/tests/rbac.enforcement.writes.batchB.test.ts
autonomous: true
requirements: [ENFORCE-06, STOCK-02]

must_haves:
  truths:
    - "No pre-existing test file constructs a SalesOrderItem payload using a free-text lotBatch string"
    - "Every pre-existing test that creates a sales order via the API now creates a real InventoryStock lot first and passes its inventoryStockId"
    - "The full pre-existing test suite (unrelated to Phase 3) still passes unmodified in behavior, only updated in fixture shape"
  artifacts: []
  key_links: []
---

<objective>
Migrate every pre-existing test file that constructs a `SalesOrderItem` payload using the old
free-text `lotBatch` string — these will fail to compile/pass against 03-04-PLAN.md's backend
otherwise, since `SalesOrderItem.lotBatch` no longer exists. `order.noSelfApproval.test.ts` was
already migrated as part of 03-07-PLAN.md (it needed simultaneous editing for the updatedById
extension); this plan covers the remaining five.

Purpose: RESEARCH.md's Validation Architecture explicitly flags this as "not a new test gap but a
mandatory migration task that must be sized into the plan's effort estimate" — leaving these broken
would silently regress the full test suite's green status.
Output: 5 test files updated to create a real `InventoryStock` lot in fixture setup and pass
`inventoryStockId` instead of `lotBatch` in every sales order item payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-backend-enforcement-lot-batch-stock-control/03-CONTEXT.md
</context>

<interfaces>
Grep-verified locations of every `lotBatch:` reference remaining after 03-07-PLAN.md's edit to
`order.noSelfApproval.test.ts` (run `grep -rn "lotBatch" apps/backend/tests` before starting to
reconfirm this list is current — 03-07/03-08's own test-writing may have introduced or resolved
additional occurrences):
- `apps/backend/tests/salesOrder.license-block.test.ts` lines ~124, 145, 163, 174-175 — sales order item payloads
- `apps/backend/tests/order.genericUpdateRestriction.test.ts` line ~141 — sales order item payload
- `apps/backend/tests/audit.crud.orders.test.ts` line ~167 — sales order item payload
- `apps/backend/tests/models.txClient.test.ts` line ~147 — sales order item payload
- `apps/backend/tests/rbac.enforcement.writes.orders.test.ts` line ~167 — sales order item payload
- `apps/backend/tests/rbac.enforcement.writes.batchB.test.ts` lines ~46, 96 — these construct an
  `InventoryStock` row directly (`prisma.inventoryStock.create({ data: { ..., lotBatch: ... } })`),
  NOT a `SalesOrderItem` payload. `InventoryStock.lotBatch` is untouched by this phase's schema
  change (only `SalesOrderItem.lotBatch` was removed) — confirm these two occurrences are indeed
  `InventoryStock` fixture creation before treating this file as needing edits; if confirmed, this
  file needs NO change and should just be re-run to confirm it still passes.

Standard fixture pattern for creating a real lot inline (same as 03-07-PLAN.md's Task 1):
```typescript
const lot = await prisma.inventoryStock.create({
  data: { productId, lotBatch: `TEST-LOT-${Date.now()}`, receivedDate: new Date(), quantityOnHand: 1000, stockAgeDays: 0, stockStatus: "NORMAL", warehouse: "Test Warehouse" },
});
// then in the sales order item payload: { productId, quantity: 2, unitPrice: 10, discount: 0, inventoryStockId: lot.inventoryStockId }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate salesOrder.license-block.test.ts and order.genericUpdateRestriction.test.ts</name>
  <read_first>
    apps/backend/tests/salesOrder.license-block.test.ts
    apps/backend/tests/order.genericUpdateRestriction.test.ts
  </read_first>
  <files>apps/backend/tests/salesOrder.license-block.test.ts, apps/backend/tests/order.genericUpdateRestriction.test.ts</files>
  <action>
For each file:
1. In the `beforeAll` fixture setup, after the product(s) are created, add a real `InventoryStock`
   lot creation per product needed by that file's test cases (using the standard fixture pattern
   above), storing the returned `inventoryStockId` in a variable scoped the same way the file
   already scopes `expiredPermitProductId`/`cleanProductId`/`salesProductId` etc.
2. Replace every `lotBatch: "LOT-1"` / `lotBatch: "LOT-2"` occurrence in a sales order item payload
   with `inventoryStockId: <the corresponding lot's id>` (matching each occurrence to the correct
   product's lot — `salesOrder.license-block.test.ts` has two distinct products, each needs its own
   lot).
3. Add the created `InventoryStock` row(s) to the file's existing `afterAll` cleanup block (same
   pattern already used for other created fixtures in that file — delete by id after the sales
   orders that reference them are deleted, respecting FK order: `salesOrder`/`salesOrderItem`
   cleanup first, then `inventoryStock`, matching `onDelete: Restrict` on the new FK).
4. Do not change any other assertion, test description, or test logic — only the fixture setup and
   item payload shape change.
  </action>
  <acceptance_criteria>
    - `grep "lotBatch" apps/backend/tests/salesOrder.license-block.test.ts` returns NO match
    - `grep "lotBatch" apps/backend/tests/order.genericUpdateRestriction.test.ts` returns NO match
    - `grep "inventoryStockId" apps/backend/tests/salesOrder.license-block.test.ts` matches at least 5 times
    - `npx vitest run tests/salesOrder.license-block.test.ts tests/order.genericUpdateRestriction.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/salesOrder.license-block.test.ts tests/order.genericUpdateRestriction.test.ts</automated>
  </verify>
  <done>Both files pass against the new schema with identical test coverage/assertions to before, only fixture shape changed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate audit.crud.orders.test.ts, models.txClient.test.ts, rbac.enforcement.writes.orders.test.ts; confirm rbac.enforcement.writes.batchB.test.ts needs no change</name>
  <read_first>
    apps/backend/tests/audit.crud.orders.test.ts
    apps/backend/tests/models.txClient.test.ts
    apps/backend/tests/rbac.enforcement.writes.orders.test.ts
    apps/backend/tests/rbac.enforcement.writes.batchB.test.ts
  </read_first>
  <files>apps/backend/tests/audit.crud.orders.test.ts, apps/backend/tests/models.txClient.test.ts, apps/backend/tests/rbac.enforcement.writes.orders.test.ts</files>
  <action>
Apply the exact same migration pattern as Task 1 to these three files: add a real `InventoryStock`
lot to each file's fixture setup, replace `lotBatch: "LOT-1"` in each sales order item payload with
`inventoryStockId: <lot id>`, add the lot to `afterAll` cleanup, change nothing else.

For `rbac.enforcement.writes.batchB.test.ts`: confirm (by reading the surrounding ~10 lines around
each of the two `lotBatch` occurrences found via grep) that both are `prisma.inventoryStock.create`
calls populating `InventoryStock.lotBatch` (the lot's own display label, untouched by this phase's
schema change), not `SalesOrderItem` payloads. If confirmed, make no code change to this file —
just re-run its test suite to confirm it still passes against the new schema (it exercises
`INVENTORY_CREATE`/`INVENTORY_EDIT` permission checks, which 03-03-PLAN.md's `quantityOnHand`
removal from `updateInventoryStock` could plausibly affect if this file's write payload includes
`quantityOnHand` — check for that too, and if a write to `quantityOnHand` via `PUT
/api/inventory-stock/:id` exists in this file expecting the value to persist, update that assertion
to reflect the new D-06 behavior — the field is now silently ignored, not rejected).
  </action>
  <acceptance_criteria>
    - `grep "lotBatch" apps/backend/tests/audit.crud.orders.test.ts apps/backend/tests/models.txClient.test.ts apps/backend/tests/rbac.enforcement.writes.orders.test.ts` returns NO match
    - `npx vitest run tests/audit.crud.orders.test.ts tests/models.txClient.test.ts tests/rbac.enforcement.writes.orders.test.ts tests/rbac.enforcement.writes.batchB.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/audit.crud.orders.test.ts tests/models.txClient.test.ts tests/rbac.enforcement.writes.orders.test.ts tests/rbac.enforcement.writes.batchB.test.ts</automated>
  </verify>
  <done>All four files pass; the three sales-order-item files are migrated to inventoryStockId, and batchB's InventoryStock-only lotBatch usage is confirmed unaffected (or fixed if a quantityOnHand-persistence assertion was found to be stale).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — test-only migration plan, no new runtime behavior introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Repudiation | A stale test asserting `quantityOnHand` persists via the generic `PUT /api/inventory-stock/:id` endpoint, silently passing against pre-03-03-PLAN.md behavior and masking a real regression | mitigate | Task 2 explicitly requires checking `rbac.enforcement.writes.batchB.test.ts` for this exact scenario and updating the assertion if found |
</threat_model>

<verification>
- `npx vitest run tests/salesOrder.license-block.test.ts tests/order.genericUpdateRestriction.test.ts tests/audit.crud.orders.test.ts tests/models.txClient.test.ts tests/rbac.enforcement.writes.orders.test.ts tests/rbac.enforcement.writes.batchB.test.ts` exits 0
- `grep -rn "lotBatch" apps/backend/tests` shows zero matches inside any SalesOrderItem payload construction (only legitimate InventoryStock.lotBatch fixture usage, if any, remains)
</verification>

<success_criteria>
- Zero pre-existing test files reference SalesOrderItem.lotBatch
- Full pre-existing test suite passes against the new schema
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-enforcement-lot-batch-stock-control/03-09-SUMMARY.md`
</output>
