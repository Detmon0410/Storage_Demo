---
phase: 03-backend-enforcement-lot-batch-stock-control
plan: 10
type: execute
wave: 6
depends_on: ["04", "05", "06", "07", "08", "09"]
files_modified:
  - apps/backend/prisma/seed.ts
autonomous: true
requirements: [STOCK-01, STOCK-04]

must_haves:
  truths:
    - "prisma db seed completes without error against the fully-migrated schema"
    - "Seeded inventory lots are created before seeded sales orders reference them by inventoryStockId"
    - "Only RECEIVED-status seeded import orders produce InventoryStock/StockTransaction rows via the model layer"
    - "The full backend test suite passes after reseeding"
  artifacts:
    - path: "apps/backend/prisma/seed.ts"
      provides: "inventoryStockId-based sales order item seeding, reordered to create lots before orders"
      contains: "inventoryStockId"
  key_links:
    - from: "apps/backend/prisma/seed.ts salesOrders seeding"
      to: "productIdByCode + lot lookup map"
      via: "inventoryStockId: lotIdByKey.get(...)"
      pattern: "inventoryStockId"
---

<objective>
Finish the D-01/D-04 migration by rewriting `prisma/seed.ts` so it creates `InventoryStock` lots
before any `SalesOrder` that references them (today's seed order creates sales orders first, then
inventory lots after — an ordering that only worked because `lotBatch` was a free string with no FK
integrity), and confirm the whole backend test suite is green end-to-end against the fully-migrated
schema. This is the last plan in the phase — its exit criterion IS the phase's exit criterion.

Purpose: Every other plan in this phase either built enforcement or tested it in isolation; this
plan proves the demo dataset itself — and the full test suite together — works against everything
built.
Output: A rewritten `seed.ts` that runs clean, and a full green `npm test` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-backend-enforcement-lot-batch-stock-control/03-CONTEXT.md
</context>

<interfaces>
Current seed.ts ordering (relevant excerpt, verified during phase planning): categories -> suppliers
-> products -> importOrders (via raw `prisma.importOrder.create` with nested items, NOT via
`ImportOrderModel.create` — this raw path bypasses the RECEIVED-gating built in 03-05-PLAN.md
entirely) -> company/licenses -> customers -> customerLicenses -> salesOrders (via raw
`prisma.salesOrder.create` with `lotBatch: item.lotBatch` string items, referencing lot label
strings that don't necessarily correspond to real rows yet) -> inventoryStocks (created LAST, via
raw `prisma.inventoryStock.create`, keyed by `productCode` + `lotBatch` string) -> dashboardKpis ->
manual stockTransactions -> then finally 3 records created via the real model layer
(`ImportOrderModel.create`, `SalesOrderModel.create`, `StockTransactionModel.create`) as a
"new-version demo" showcase at the very end of `main()`.

`inventoryStocks` seed array shape: `["SKU-1001", "LOT-A2607-01", "2026-07-20", 360, 43, "NORMAL",
"Warehouse A - Tokyo", "IMP-2026-0151"]` = `[productCode, lotBatch, receivedDate, quantityOnHand,
stockAgeDays, stockStatus, warehouse, sourceImportOrderNo]`.

`salesOrders` seed array shape: items currently `{ productCode, quantity, unitPrice, discount,
lotBatch }` — `lotBatch` values like `"LOT-A2607-01"` are designed to match an `inventoryStocks`
row's `lotBatch` for the same `productCode` (e.g. SO-2026-3301's first item uses `LOT-A2607-01`,
which matches the `inventoryStocks` row `["SKU-1001", "LOT-A2607-01", ...]`) — this matching
relationship already exists in the data, it just needs to be resolved to a real FK instead of a
string comparison at read time.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reorder seed.ts — create inventory lots before sales orders, convert lotBatch strings to inventoryStockId lookups, gate import-order stock creation to RECEIVED</name>
  <read_first>
    apps/backend/prisma/seed.ts
    apps/backend/src/models/importOrder.model.ts
    apps/backend/src/models/salesOrder.model.ts
  </read_first>
  <files>apps/backend/prisma/seed.ts</files>
  <action>
Edit `apps/backend/prisma/seed.ts`:

1. Move the `inventoryStocks` creation block to run immediately after the `importOrders` block (and
   its `importOrderItemIdByKey` map is built) and BEFORE the `salesOrders` creation block. No change
   to the `inventoryStocks` array data itself or its creation loop body — only its position in
   `main()` moves earlier.

2. While building the `inventoryStocks` loop, also populate a new lookup map (parallel to the
   existing `importOrderItemIdByKey` pattern):
   ```typescript
   const lotIdByKey = new Map<string, number>(); // `${productCode}::${lotBatch}` -> inventoryStockId
   ```
   populated inside that same loop as `lotIdByKey.set(`${productCode}::${lotBatch}`, created.inventoryStockId);`
   after each `prisma.inventoryStock.create(...)` call.

3. In the `salesOrders` creation block's `itemRows` construction, change
   `lotBatch: item.lotBatch,` to `inventoryStockId: lotIdByKey.get(`${item.productCode}::${item.lotBatch}`)!,`
   — the existing `salesOrders` array's `item.lotBatch` values already match real `inventoryStocks`
   rows for the same `productCode` (verified during planning — e.g. SO-2026-3301's
   `LOT-A2607-01` matches the SKU-1001 lot of the same label), so this lookup should always resolve.
   If any lookup returns `undefined` for a given order (meaning the seed data's lot/product pairing
   doesn't actually exist), throw a clear `Error` naming the offending `orderNo`/`productCode`/`lotBatch`
   combination rather than silently inserting `undefined` — this surfaces any stale seed data
   immediately instead of producing a cryptic Prisma FK error.

4. The three "new-version demo" records at the end of `main()` (`ImportOrderModel.create`,
   `SalesOrderModel.create`, `StockTransactionModel.create`) already route through the real model
   layer, so they automatically pick up 03-04/03-05-PLAN.md's new behavior. Update their literal
   `lotBatch: "LOT-A2608-05"` / `lotBatch: "LOT-A2607-02"` item fields to real `inventoryStockId`
   values: the second one (`LOT-A2607-02`) already exists as a seeded `SKU-1010` lot — resolve it
   via `lotIdByKey.get("SKU-1010::LOT-A2607-02")`. The first (`LOT-A2608-05` for `SKU-1012`) does
   not exist in the `inventoryStocks` seed array — since this showcase sales order is meant to
   demonstrate the live model-layer flow, create a small dedicated `InventoryStock` row for it
   first (`prisma.inventoryStock.create({ data: { productId: productIdByCode.get("SKU-1012")!,
   lotBatch: "LOT-A2608-05", receivedDate: date("2026-08-13"), quantityOnHand: 200, stockAgeDays: 5,
   stockStatus: "NORMAL", warehouse: "Warehouse A - Tokyo" } })`) immediately before this showcase
   block, and use its `inventoryStockId`.

5. The showcase `ImportOrderModel.create({ ..., status: "RECEIVED", ... })` call already passes
   `status: "RECEIVED"` — confirm (no code change needed if so) that this now correctly creates a
   real `InventoryStock` lot + stock-IN transaction via 03-05-PLAN.md's gating logic, in addition to
   the manually-seeded `inventoryStocks` array above. This demonstrates both seeding paths
   (bulk historical data via raw inserts, and the live receiving flow via the model layer)
   coexisting correctly.

6. Do not change the raw `importOrders` array/creation loop itself (it stays a raw insert for bulk
   historical data, matching its existing "scene-setting" comment) — only the `inventoryStocks`
   block's position and the `salesOrders` block's item-field mapping change.
  </action>
  <acceptance_criteria>
    - `grep "lotIdByKey" apps/backend/prisma/seed.ts` matches at least 3 times (declaration, population, lookup)
    - `grep -B2 "const inventoryStocks = \[" apps/backend/prisma/seed.ts` (the array literal) is unchanged in content
    - In `main()`, the line number of the `inventoryStocks` creation loop is now LESS than the line number of the `salesOrders` creation loop (grep both, compare line numbers)
    - `grep "lotBatch: item.lotBatch" apps/backend/prisma/seed.ts` returns NO match
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npm run db:seed</automated>
  </verify>
  <done>Seed runs successfully end-to-end with inventory lots created before sales orders reference them, and every sales order item resolves a real inventoryStockId.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2 [BLOCKING]: Full backend test suite verification</name>
  <read_first>
    apps/backend/package.json
  </read_first>
  <files>none (verification-only task)</files>
  <action>
Run the full backend test suite from `apps/backend`:
```
npm test
```
This exercises every test file touched or added across all of 03-01 through 03-09-PLAN.md, plus the
full pre-existing suite (auth, RBAC, audit, permit-status tests from Phases 1/2/7 that this phase
did not touch). If any test fails:
1. Determine whether the failure is a genuine regression from this phase's changes (most likely: a
   test file this plan set did not anticipate touching lotBatch, or a KPI/dashboard test whose
   expected `stockQty` numbers assumed the pre-D-04 unconditional stock-IN behavior — RESEARCH.md's
   Runtime State Inventory explicitly flagged this risk for `DashboardKpi`/hardcoded quantity
   assertions).
2. Fix the specific failing assertion (update the expected stock quantity to match the new
   RECEIVED-gated reality, or migrate a missed `lotBatch` reference) rather than loosening the
   assertion or skipping the test.
3. Re-run `npm test` until it is fully green.
Report the final pass/fail count and any fixes made in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `npm test` (from apps/backend) exits 0
    - Zero test files remain that reference `SalesOrderItem.lotBatch`
    - No `.only`/`.skip` was added to any test to force a pass
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npm test</automated>
  </verify>
  <done>Full backend test suite is green. Phase 3 is functionally complete: every ENFORCE-01..06 and STOCK-01..06 requirement is implemented and tested, the schema migration is applied, and the demo seed data reflects the new lot-based reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — seed/verification plan, no new runtime trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-24 | Repudiation | A dashboard/KPI test silently passing with stale expected stock totals that no longer reflect RECEIVED-gated reality | mitigate | Task 2 explicitly instructs fixing (not loosening) any such assertion found during the full-suite run |
| T-03-25 | Denial of Service (data integrity) | Seed script throwing a cryptic Prisma FK error mid-run, leaving the dev database in a partially-seeded state | mitigate | Task 1's lotIdByKey lookup explicitly throws a clear, named error before any Prisma FK violation would occur, if seed data pairing is ever stale |
</threat_model>

<verification>
- `npm run db:seed` (from apps/backend) exits 0
- `npm test` (from apps/backend) exits 0 with zero failures
</verification>

<success_criteria>
- Phase 3 is complete: all 12 requirement IDs (ENFORCE-01..06, STOCK-01..06) are implemented, tested, and passing
- Seed data is fully migrated to the lot-based FK model
- No regressions in the pre-existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-enforcement-lot-batch-stock-control/03-10-SUMMARY.md`
</output>
