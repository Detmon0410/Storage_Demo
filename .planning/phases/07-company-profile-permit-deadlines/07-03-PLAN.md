---
phase: 07
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/backend/src/utils/licenseGate.ts
  - apps/backend/src/models/salesOrder.model.ts
  - apps/backend/src/models/importOrder.model.ts
  - apps/backend/tests/salesOrder.license-block.test.ts
  - apps/backend/tests/importOrder.license-block.test.ts
autonomous: true
requirements: [PERMIT-04]

must_haves:
  truths:
    - "Creating a sales order for a product whose linked License is expired is rejected by the backend with a 400, even if called directly via API"
    - "Creating an import order for a product whose linked License is expired is rejected by the backend with a 400"
    - "Editing an existing sales/import order's line items to add an expired-permit product is also rejected, not just order creation"
    - "The block happens inside the same Prisma $transaction as the order write — no partial rows are written when blocked"
  artifacts:
    - path: "apps/backend/src/utils/licenseGate.ts"
      provides: "assertProductsNotBlockedTx(tx, productIds) shared transactional pre-check"
      exports: ["assertProductsNotBlockedTx"]
  key_links:
    - from: "apps/backend/src/models/salesOrder.model.ts"
      to: "apps/backend/src/utils/licenseGate.ts"
      via: "assertProductsNotBlockedTx(tx, items.map(i => i.productId)) inside $transaction"
      pattern: "assertProductsNotBlockedTx"
    - from: "apps/backend/src/models/importOrder.model.ts"
      to: "apps/backend/src/utils/licenseGate.ts"
      via: "assertProductsNotBlockedTx(tx, items.map(i => i.productId)) inside $transaction"
      pattern: "assertProductsNotBlockedTx"
---

<objective>
Implement the backend-enforced order-blocking gate for PERMIT-04: any product with a linked, EXPIRED License cannot be added to a new or edited import/sales order. The check runs inside the same Prisma transaction as the order write, mirroring the existing `validateAndSnapshotLicense` pattern in `salesOrder.model.ts`.

Purpose: This is the core-value enforcement point for the phase — "prevent invalid business actions at the backend, not just hide buttons in the UI" (REQUIREMENTS.md line 4) — applied to the new product-permit relationship from Plan 07-02.
Output: A shared `assertProductsNotBlockedTx` helper, wired into all 4 mutation entry points (import create/update, sales create/update), with integration tests proving both order types and both create/update paths are covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-company-profile-permit-deadlines/07-RESEARCH.md
@.planning/phases/07-company-profile-permit-deadlines/07-PATTERNS.md
@apps/backend/src/models/salesOrder.model.ts
@apps/backend/src/models/importOrder.model.ts
</context>

<interfaces>
<!-- From Plan 07-01 (already implemented): -->
```typescript
// apps/backend/src/utils/permitStatus.ts
export function computePermitStatus(expiryDate: Date, today?: Date): { daysRemaining: number; status: "PREPARATION" | "NOTIFY" | "WARNING" | "IMPORTANT_WARNING" | "EXPIRED" | "NORMAL" };
```
Schema: `License.productId Int?` — a License row may be linked to a specific `Product`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assertProductsNotBlockedTx shared helper</name>
  <files>apps/backend/src/utils/licenseGate.ts</files>
  <read_first>
    apps/backend/src/models/salesOrder.model.ts (lines 60-71, `validateAndSnapshotLicense` — the exact pattern to mirror)
    apps/backend/src/utils/permitStatus.ts
  </read_first>
  <behavior>
    - Test: given a `tx` where `license.findMany` (mocked or real via a test transaction) returns a License row with `productId: 5` and `expiryDate` in the past, calling `assertProductsNotBlockedTx(tx, [5])` throws an `HttpError` with status 400 and a message mentioning the product
    - Test: given no License rows reference any of the passed `productIds`, `assertProductsNotBlockedTx` resolves without throwing
    - Test: given a License row with `productId` matching but `expiryDate` in the future, `assertProductsNotBlockedTx` resolves without throwing (not EXPIRED)
    - Test: passing an empty `productIds` array resolves immediately without querying
  </behavior>
  <action>
    Create `apps/backend/src/utils/licenseGate.ts`:
    ```typescript
    import type { Prisma } from "@prisma/client";
    import { HttpError } from "../middleware/errorHandler.js";
    import { computePermitStatus } from "./permitStatus.js";

    export const assertProductsNotBlockedTx = async (tx: Prisma.TransactionClient, productIds: number[]) => {
      const uniqueIds = [...new Set(productIds)].filter((id) => Number.isFinite(id));
      if (uniqueIds.length === 0) return;

      const licenses = await tx.license.findMany({ where: { productId: { in: uniqueIds } } });
      for (const license of licenses) {
        const { status } = computePermitStatus(license.expiryDate);
        if (status === "EXPIRED") {
          throw new HttpError(400, `Product ${license.productId} has an expired permit (license ${license.licenseNo}) and cannot be ordered until it is resolved`);
        }
      }
    };
    ```
    This function MUST be called with a `Prisma.TransactionClient` (`tx`), never the top-level `prisma` client — it is designed to run inside `prisma.$transaction(async (tx) => {...})` blocks so that a thrown `HttpError` aborts the whole transaction with zero partial writes, matching the existing `validateAndSnapshotLicense` idiom.

    Add unit/integration coverage for this function either as its own test file (`apps/backend/tests/licenseGate.test.ts`, using `prisma.$transaction` in a real test against a throwaway product+license row, then rolling back or cleaning up) OR fold the coverage directly into Task 2/3's integration tests below — if folded in, note this explicitly in the plan SUMMARY so verification isn't double-counted.
  </action>
  <acceptance_criteria>
    - `apps/backend/src/utils/licenseGate.ts` exports `assertProductsNotBlockedTx` with signature `(tx: Prisma.TransactionClient, productIds: number[]) => Promise<void>`
    - The function throws `HttpError(400, ...)` when any passed `productId` has an EXPIRED-status License linked to it
    - The function is a no-op (resolves, no query) when `productIds` is empty
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/salesOrder.license-block.test.ts tests/importOrder.license-block.test.ts</automated>
  </verify>
  <done>Shared blocking helper exists and is ready to wire into both order models.</done>
</task>

<task type="auto">
  <name>Task 2: Wire blocking gate into SalesOrderModel (create + update)</name>
  <files>apps/backend/src/models/salesOrder.model.ts, apps/backend/tests/salesOrder.license-block.test.ts</files>
  <read_first>
    apps/backend/src/models/salesOrder.model.ts (full file — `create` lines 86-115, `update` lines 117-173, especially the items-provided branch at lines 144-172)
    apps/backend/tests/auth.enforcement.test.ts (supertest + createTestUser pattern)
    apps/backend/prisma/seed.ts (lines 254-264, 275+ — for realistic Category/Supplier/Product/Customer/CustomerLicense field shapes to build test fixtures)
  </read_first>
  <action>
    In `apps/backend/src/models/salesOrder.model.ts`:
    1. Import `assertProductsNotBlockedTx` from `../utils/licenseGate.js`.
    2. In `SalesOrderModel.create`, inside the `prisma.$transaction(async (tx) => {...})` callback, call `await assertProductsNotBlockedTx(tx, data.items.map((i) => i.productId));` as the FIRST statement in the callback, before `validateAndSnapshotLicense`.
    3. In `SalesOrderModel.update`, inside the `items`-provided branch's `prisma.$transaction(async (tx) => {...})` callback (the branch starting at `const rows = toItemRows(items);`), call `await assertProductsNotBlockedTx(tx, items.map((i) => i.productId));` as the FIRST statement in that callback, before `reverseAndDeleteByReferenceTx`. The non-`items` branch (only `customerLicenseId`/order-field changes) does NOT need this check — it never touches product line items.

    Create `apps/backend/tests/salesOrder.license-block.test.ts`:
    - Setup: `createTestUser`, login for `accessToken`. Directly via `prisma` (imported from `tests/setup.js`): create a `Category`, `Supplier`, `Product` (status `"READY"`, sufficient `stockQty`), `Customer`, `CustomerLicense` (status `ACTIVE`, `expiryDate` far in the future), and a `License` row with `productId` set to the created Product's id and `expiryDate` in the past (e.g. `new Date(Date.now() - 10 * 86400000)`).
    - Test: `POST /api/sales-orders` with a line item referencing that expired-permit product returns 400, and the response body's error message mentions "expired permit".
    - Test: no `SalesOrder` row is created as a result (verify via `prisma.salesOrder.findUnique({ where: { orderNo } })` returns `null`).
    - Test: the same request against a product with NO linked License, or a License with a future `expiryDate`, returns 201.
    - Test: `PUT /api/sales-orders/:id` that adds a new line item for the expired-permit product to an existing (previously valid) order also returns 400.
    - Clean up all created rows in `afterAll`/`afterEach`.
  </action>
  <acceptance_criteria>
    - `apps/backend/src/models/salesOrder.model.ts` imports and calls `assertProductsNotBlockedTx` in both `create` and the items branch of `update`
    - `npx vitest run tests/salesOrder.license-block.test.ts` (from `apps/backend/`) passes
    - Test asserts no `SalesOrder` row is persisted when the block fires (transaction rollback proof)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/salesOrder.license-block.test.ts</automated>
  </verify>
  <done>Sales order create and update-with-items both reject expired-permit products; verified end-to-end via supertest against the real app + database.</done>
</task>

<task type="auto">
  <name>Task 3: Wire blocking gate into ImportOrderModel (create + update)</name>
  <files>apps/backend/src/models/importOrder.model.ts, apps/backend/tests/importOrder.license-block.test.ts</files>
  <read_first>
    apps/backend/src/models/importOrder.model.ts (full file — `create` lines 67-101, `update` lines 103-146, especially the items-provided branch at lines 123-145)
  </read_first>
  <action>
    In `apps/backend/src/models/importOrder.model.ts`:
    1. Import `assertProductsNotBlockedTx` from `../utils/licenseGate.js`.
    2. In `ImportOrderModel.create`, inside the `prisma.$transaction(async (tx) => {...})` callback, call `await assertProductsNotBlockedTx(tx, data.items.map((i) => i.productId));` as the FIRST statement, before `tx.importOrder.create`.
    3. In `ImportOrderModel.update`, the items-provided branch already wraps its work in `prisma.$transaction(async (tx) => {...})` (starting at `const rows = toItemRows(items);`). Add `await assertProductsNotBlockedTx(tx, items.map((i) => i.productId));` as the FIRST statement in that callback, right after the `if (!existing) throw new HttpError(404, ...)` check and before `reverseAndDeleteByReferenceTx`. The non-`items` branch (`if (!items) { return prisma.importOrder.update(...) }`) is NOT wrapped in a transaction today and does not touch line items — leave it unchanged, no check needed.

    Create `apps/backend/tests/importOrder.license-block.test.ts` (mirror the structure of `salesOrder.license-block.test.ts` from Task 2, adapted for import orders — no CustomerLicense/Customer needed, just Category/Supplier/Product/License):
    - Test: `POST /api/import-orders` with a line item referencing an expired-permit product returns 400, error message mentions "expired permit"; no `ImportOrder` row is created.
    - Test: the same request for a product with no License link, or a future-expiry License, returns 201.
    - Test: `PUT /api/import-orders/:id` that adds an expired-permit product line item to an existing order returns 400.
    - Clean up created rows in `afterAll`/`afterEach`.
  </action>
  <acceptance_criteria>
    - `apps/backend/src/models/importOrder.model.ts` imports and calls `assertProductsNotBlockedTx` in both `create` and the items branch of `update`
    - `npx vitest run tests/importOrder.license-block.test.ts` (from `apps/backend/`) passes
    - Test asserts no `ImportOrder` row is persisted when the block fires
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/importOrder.license-block.test.ts</automated>
  </verify>
  <done>Import order create and update-with-items both reject expired-permit products — closing RESEARCH.md Pitfall 2 (import orders previously had zero license-awareness).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated client → `POST`/`PUT /api/sales-orders`, `/api/import-orders` | A caller bypassing the frontend UI entirely (direct API call) must still be blocked from ordering an expired-permit product |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering / Elevation of Privilege | `salesOrder.model.ts` / `importOrder.model.ts` create+update | mitigate | `assertProductsNotBlockedTx` runs inside the same `prisma.$transaction` as the order write in ALL 4 mutation entry points (sales create, sales update-with-items, import create, import update-with-items) — a direct API call cannot skip the check because there is no code path that writes order items without first entering the guarded transaction |
| T-07-07 | Tampering (partial write) | `$transaction` rollback behavior | mitigate | `HttpError` thrown inside the `tx` callback aborts the entire Prisma transaction — verified by tests asserting no `SalesOrder`/`ImportOrder` row exists after a blocked attempt |
| T-07-08 | Repudiation | frontend-only enforcement (non-authoritative) | accept | The frontend blocker UI (Plan 07-08) is a UX convenience only; this plan's backend gate is the sole authoritative enforcement point, satisfying the project's Core Value statement |
</threat_model>

<verification>
`cd apps/backend && npm test` — full suite green, including both new `*.license-block.test.ts` files. Confirm via test assertions (not just HTTP status) that no order row is persisted when a block fires.
</verification>

<success_criteria>
- Both `SalesOrderModel` and `ImportOrderModel` reject create/update-with-items for any product with an EXPIRED-status linked License
- The block is enforced inside the same database transaction as the order write
- Tests prove both order types × both create/update paths are covered (4 call sites total)
</success_criteria>

<output>
After completion, create `.planning/phases/07-company-profile-permit-deadlines/07-03-SUMMARY.md`
</output>
