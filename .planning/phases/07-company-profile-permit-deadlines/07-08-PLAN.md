---
phase: 07
plan: 08
type: execute
wave: 4
depends_on: ["07-03", "07-04"]
files_modified:
  - apps/frontend/src/pages/SalesOrdersPage.tsx
  - apps/frontend/src/pages/ImportOrdersPage.tsx
autonomous: false
requirements: [PERMIT-04]

must_haves:
  truths:
    - "A user attempting to add an expired-permit product to a sales order sees a clear blocker message and cannot save"
    - "A user attempting to add an expired-permit product to an import order sees the same blocker and cannot save"
  artifacts:
    - path: "apps/frontend/src/pages/SalesOrdersPage.tsx"
      provides: "extended validation.blockers array including expired-permit check"
    - path: "apps/frontend/src/pages/ImportOrdersPage.tsx"
      provides: "new validation.blockers array (previously did not exist) including expired-permit check"
  key_links:
    - from: "apps/frontend/src/pages/SalesOrdersPage.tsx"
      to: "apps/frontend/src/api/resources.ts"
      via: "licenseApi.list() to check product-linked license status"
      pattern: "licenseApi\\.list"
    - from: "apps/frontend/src/pages/ImportOrdersPage.tsx"
      to: "apps/frontend/src/api/resources.ts"
      via: "licenseApi.list() to check product-linked license status"
      pattern: "licenseApi\\.list"
---

<objective>
Surface PERMIT-04's expired-permit order block in both order-entry UIs: extend `SalesOrdersPage.tsx`'s existing `validation.blockers` array, and introduce the same blocker pattern (net new) in `ImportOrdersPage.tsx`, which currently has no blockers/warnings UI at all.

Purpose: This is the UX layer over Plan 07-03's authoritative backend gate — a non-authoritative convenience so users see the block before submitting, not just after a failed API call. The backend (Plan 07-03) remains the sole authoritative enforcement point.
Output: Updated `SalesOrdersPage.tsx` and `ImportOrdersPage.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-company-profile-permit-deadlines/07-UI-SPEC.md
@.planning/phases/07-company-profile-permit-deadlines/07-PATTERNS.md
@apps/frontend/src/pages/SalesOrdersPage.tsx
@apps/frontend/src/pages/ImportOrdersPage.tsx
</context>

<interfaces>
<!-- From Plan 07-02/07-03 (already implemented): License API responses include `productId`/`status`; backend rejects orders for EXPIRED-permit-linked products inside the same transaction. -->
<!-- From Plan 07-04 (already implemented): i18n keys `salesOrder.validation.permitExpired`, `importOrder.validation.permitExpired`, both = "This product's permit has expired. Renew the permit before creating new orders for it." -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SalesOrdersPage.tsx — extend validation.blockers with expired-permit check</name>
  <files>apps/frontend/src/pages/SalesOrdersPage.tsx</files>
  <read_first>
    apps/frontend/src/pages/SalesOrdersPage.tsx (full file — `validation` useMemo lines 112-150, `quantityByProduct` lines 96-103, blocker-row rendering lines 551-556)
  </read_first>
  <action>
    Edit `apps/frontend/src/pages/SalesOrdersPage.tsx`:
    1. Add `import { customerApi, customerLicenseApi, inventoryStockApi, licenseApi, productApi, salesOrderApi } from "../api/resources";` (add `licenseApi` to the existing import list).
    2. Add `const licenses = useList(() => licenseApi.list());` alongside the other `useList(...)` calls near the top of the component.
    3. In the `validation` `useMemo` (lines 112-150), inside the existing `for (const [productId, qty] of quantityByProduct)` loop (which already checks `product.status !== "READY"` and stock), add an additional check:
    ```typescript
    const linkedLicense = licenses.find((l) => l.productId != null && String(l.productId) === productId);
    if (linkedLicense?.status === "EXPIRED") {
      blockers.push(t("salesOrder.validation.permitExpired"));
    }
    ```
    4. Add `licenses` to the `useMemo`'s dependency array (currently `[quantityByProduct, products, selectedCustomer, validLicensesForCustomer, form.customerLicenseId, form.items, form.approver, orderTotal, t]` — append `licenses`).
    5. No changes needed to the blocker-row rendering JSX (lines 551-556) — it already maps over `validation.blockers` generically.
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/pages/SalesOrdersPage.tsx` imports `licenseApi` and calls `useList(() => licenseApi.list())`
    - The `validation` `useMemo` pushes `t("salesOrder.validation.permitExpired")` to `blockers` when a line item's product has a linked License with `status === "EXPIRED"`
    - `licenses` is included in the `useMemo` dependency array
    - `cd apps/frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Sales order form blocks (via existing rose ShieldAlert blocker UI) any line item for a product whose linked permit is expired.</done>
</task>

<task type="auto">
  <name>Task 2: ImportOrdersPage.tsx — introduce validation.blockers with expired-permit check</name>
  <files>apps/frontend/src/pages/ImportOrdersPage.tsx</files>
  <read_first>
    apps/frontend/src/pages/ImportOrdersPage.tsx (full file — currently has NO validation/blockers infrastructure at all)
    apps/frontend/src/pages/SalesOrdersPage.tsx (lines 112-150 validation useMemo structure, lines 551-556 blocker-row rendering — the exact pattern to port, since ImportOrdersPage has no precedent)
  </read_first>
  <action>
    Edit `apps/frontend/src/pages/ImportOrdersPage.tsx`:
    1. Add `ShieldAlert` to the existing `lucide-react` import list. Add `import { licenseApi } from "../api/resources";` (add to the existing `importOrderApi, productApi, supplierApi` import).
    2. Add `const licenses = useList(() => licenseApi.list());` alongside the other `useList(...)` calls.
    3. Add a new `validation` `useMemo`, ported from `SalesOrdersPage.tsx`'s structure but scoped only to the expired-permit check (this page has no stock/sellable-status gating today — do not add any check beyond the permit block):
    ```typescript
    const validation = useMemo(() => {
      const blockers: string[] = [];
      const productIds = new Set(form.items.map((item) => item.productId).filter(Boolean));
      for (const productId of productIds) {
        const linkedLicense = licenses.find((l) => l.productId != null && String(l.productId) === productId);
        if (linkedLicense?.status === "EXPIRED") {
          blockers.push(t("importOrder.validation.permitExpired"));
        }
      }
      return { blockers };
    }, [form.items, licenses, t]);
    ```
    4. In `handleSubmit`, after the existing required-field/items checks and before building `payload`, add:
    ```typescript
    if (validation.blockers.length > 0) {
      toast.error(validation.blockers[0]);
      return;
    }
    ```
    5. In the `Modal`'s JSX, after the line-items summary block (mirroring `SalesOrdersPage.tsx` lines 551-556) and before the closing `</div>` of the `space-y-5` wrapper, add:
    ```tsx
    {validation.blockers.map((msg) => (
      <div key={msg} className="flex items-start gap-2 rounded-lg border border-rose-200 bg-rose-50 px-3 py-2 text-xs text-rose-700">
        <ShieldAlert className="mt-0.5 h-3.5 w-3.5 shrink-0" />
        <span>{msg}</span>
      </div>
    ))}
    ```
    6. Also disable the Save button when blocked, mirroring `SalesOrdersPage.tsx`'s `disabled={!canSubmit}` pattern: change the primary `Button` in the modal footer to `disabled={validation.blockers.length > 0}`.
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/pages/ImportOrdersPage.tsx` imports `licenseApi` and `ShieldAlert`, and calls `useList(() => licenseApi.list())`
    - A `validation` `useMemo` exists producing `{ blockers: string[] }`, pushing `t("importOrder.validation.permitExpired")` for any line item's product with an EXPIRED linked License
    - `handleSubmit` returns early with a toast error when `validation.blockers.length > 0`
    - The modal footer's Save `Button` has `disabled={validation.blockers.length > 0}`
    - The blocker rows render using the exact `rounded-lg border border-rose-200 bg-rose-50 px-3 py-2 text-xs text-rose-700` class structure with a `ShieldAlert` icon, matching `SalesOrdersPage.tsx` verbatim
    - `cd apps/frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Import order form now has a blockers UI (previously absent) that blocks expired-permit-linked products, visually matching the sales order pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — order-blocking UI in both order forms</name>
  <action>Manually verify the order-blocking UI against the steps in `<how-to-verify>` below; this task has no automated check by design (visual/interactive UI confirmation).</action>
  <what-built>Order-blocking UI for expired permits in both Sales Orders and Import Orders forms — a rose blocker message that appears and disables Save when a line item's product has an expired, product-linked permit.</what-built>
  <how-to-verify>
    1. In the Licenses page, ensure at least one License is linked to a specific Product (via Plan 07-06's Product select) with an expiry date in the past.
    2. Go to Sales Orders → Create — add a line item for that product. Confirm the rose "This product's permit has expired..." message appears and Save is disabled/blocked.
    3. Remove that line item or swap to a non-blocked product — confirm the message disappears and Save becomes available again (assuming other validations pass).
    4. Repeat steps 2-3 in Import Orders → Create.
    5. Confirm that attempting to bypass the UI is still rejected by the backend (e.g. re-verify via the automated tests from Plan 07-03, or attempt a direct API call) — the frontend block is a convenience, not the actual enforcement.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser form validation | Non-authoritative — purely a UX convenience layer over Plan 07-03's backend transactional gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering (client bypass) | `SalesOrdersPage.tsx` / `ImportOrdersPage.tsx` blockers | accept | This is explicitly a non-authoritative UI convenience; a client that bypasses this check (disabled JS, direct API call, modified bundle) is still rejected by the backend transactional gate from Plan 07-03 (T-07-06/T-07-07), which is the sole authoritative enforcement point |
</threat_model>

<verification>
`cd apps/frontend && npx tsc --noEmit`. Manual checkpoint verification per the `checkpoint:human-verify` task above. Cross-reference: the backend tests from Plan 07-03 (`salesOrder.license-block.test.ts`, `importOrder.license-block.test.ts`) remain the authoritative proof of enforcement.
</verification>

<success_criteria>
- Both order-entry UIs show a clear, consistent blocker message and prevent Save when a line item's product has an expired permit
- The UI-level block never contradicts the backend's actual enforcement decision
</success_criteria>

<output>
After completion, create `.planning/phases/07-company-profile-permit-deadlines/07-08-SUMMARY.md`
</output>
