---
phase: 04-approval-workflow
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/backend/prisma/seed.ts
autonomous: true
requirements: [APPROVAL-01]

must_haves:
  truths:
    - "Seed data compiles and runs against the split logisticsStatus/status schema, with no remaining references to the removed approver/requiresApproval fields"
    - "Every seeded import order has a plausible logisticsStatus (pipeline-only value) and a plausible status (approval value)"
    - "Every seeded sales order has a plausible status (approval value) with deliveryStatus retaining only its pipeline values"
  artifacts:
    - path: "apps/backend/prisma/seed.ts"
      provides: "Reseedable demo data matching the Phase 4 schema split"
      contains: "logisticsStatus:"
  key_links:
    - from: "apps/backend/prisma/seed.ts"
      to: "apps/backend/prisma/schema.prisma"
      via: "npx prisma db seed"
      pattern: "logisticsStatus"
---

<objective>
Update the demo seed data so it compiles and runs against the Phase 4 schema split, replacing every
`approver`/`requiresApproval` reference and every overloaded `ImportOrder.status` value with the new
`logisticsStatus` + `status` (OrderStatus) pair, then actually reseed the dev database so downstream
plans (test sweep, frontend, final verification) run against realistic, consistent demo data.

Purpose: `prisma/seed.ts` currently sets `approver: "..."` on ~14 import-order rows and ~9 sales-order
rows, and sets `ImportOrder.status` to values that no longer exist on that renamed field. None of this
compiles against the Phase 4 client types from 04-01/04-02. This is data-shape work, not new business
logic — every mapping decision here is this plan's own reasonable inference (RESEARCH.md flags its
own suggested mapping as [ASSUMED]/unconfirmed), applied consistently across all seed rows.
Output: Updated seed.ts, successfully reseeded dev database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-approval-workflow/04-CONTEXT.md
@.planning/phases/04-approval-workflow/04-02-SUMMARY.md
</context>

<interfaces>
From apps/backend/src/models/importOrder.model.ts (after 04-02): `ImportOrderModel.create()`'s data
parameter uses `logisticsStatus` (client-facing pipeline field, no `approver`); `status` (approval) is
always server-derived from `assertImportValueThresholdTx`, never accepted as a create parameter.

From apps/backend/src/models/salesOrder.model.ts (after 04-02): `SalesOrderModel.create()`'s data
parameter no longer accepts `approver`; `status` is always server-derived from `applyLotGuardsTx`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update seed.ts's import-order and sales-order data blocks</name>
  <read_first>
    apps/backend/prisma/seed.ts
  </read_first>
  <files>apps/backend/prisma/seed.ts</files>
  <action>
1. In the `importOrders` array literal (lines 85-252), remove the `approver: "..."` line from every
   entry and rename each entry's `status: "..."` key to `logisticsStatus: "..."`, remapping the value
   per this exact table (derived from RESEARCH.md's own suggested mapping, since `ISSUE` orders have
   already passed customs and are flagged post-receipt, not pre-approval):
   | Old `status` value | New `logisticsStatus` value |
   |---|---|
   | `STAGING` | `STAGING` |
   | `PENDING_APPROVAL` | `STAGING` |
   | `APPROVED` | `STAGING` |
   | `CUSTOMS_CLEARED` | `CUSTOMS_CLEARED` |
   | `RECEIVED` | `RECEIVED` |
   | `ISSUE` | `ISSUE` |
   Concretely: `IMP-2026-0143` (`PENDING_APPROVAL`) and `IMP-2026-0152` (`APPROVED`) both become
   `logisticsStatus: "STAGING"`; every `RECEIVED`/`CUSTOMS_CLEARED`/`ISSUE`/`STAGING` entry keeps its
   existing value verbatim under the new key name.

2. Where the `importOrders` array is consumed (the `for (const order of importOrders)` loop, ~line
   710-746), replace the `prisma.importOrder.create({ data: { ... status: order.status, approver:
   nullable(order.approver), ... } })` call's two lines:
   ```typescript
        status: order.status,
        approver: nullable(order.approver),
   ```
   with:
   ```typescript
        logisticsStatus: order.logisticsStatus,
   ```
   (`status` (OrderStatus) is intentionally omitted here — it defaults to `APPROVED` per the schema's
   `@default(APPROVED)`, which is the correct historical assumption for all seeded demo data: none of
   this pre-existing demo data represents a pending approval scenario. If a specific row needs to
   demonstrate `PENDING_APPROVAL`/`REJECTED`, add `status: "PENDING_APPROVAL"` etc. explicitly to that
   row's `data:` block as a one-off, but do not add it to every row.)

3. In the `salesOrders` array literal (lines 398-481), remove the `approver: "..."` line from every
   entry (the field is entirely gone — do not replace it with anything; `deliveryStatus` already
   carries the pipeline value and is untouched by this rename).

4. Where the `salesOrders` array is consumed (~line 882-901), remove the line:
   ```typescript
        approver: nullable(order.approver),
   ```
   from the `prisma.salesOrder.create({ data: { ... } })` call. Leave `status` unset (defaults to
   `APPROVED` per schema, matching every seeded sales order's existing delivered/shipped/pending
   history — none of these rows represent a pending-approval scenario either).

5. At the bottom of `main()`, the two model-layer showcase calls need their field names updated too:
   - `ImportOrderModel.create({ ..., status: "RECEIVED", approver: "Kumiko Sato (Manager)", ... })`
     (around line 929-939) becomes `ImportOrderModel.create({ ..., logisticsStatus: "RECEIVED", ... })`
     (drop `approver` entirely — this call goes through the real model, which will independently
     compute `status: "APPROVED"` since this showcase order's total value is well under
     `IMPORT_ORDER_APPROVAL_THRESHOLD`).
   - `SalesOrderModel.create({ ..., deliveryStatus: "PENDING", ... })` (around line 957-981) needs no
     change — it never set `approver` or `requiresApproval` to begin with.

6. Update the `nullable()` helper's remaining call sites: after steps 2-4, `nullable(order.approver)`
   no longer appears anywhere. If `nullable()` becomes unused elsewhere in the file (check with grep
   before removing), leave the helper function declaration in place only if it's still referenced by
   something else (e.g. `nullable(order.customsEntryNo)` at the import-order create call, which is
   unrelated to this phase and must stay); do not remove `nullable()` itself, only its
   `order.approver` call sites.
  </action>
  <acceptance_criteria>
    - `grep -n "approver" apps/backend/prisma/seed.ts` returns NO matches
    - `grep -n "requiresApproval" apps/backend/prisma/seed.ts` returns NO matches
    - `grep -c "logisticsStatus" apps/backend/prisma/seed.ts` returns at least 13 (12 import-order array entries + the create-call field name)
    - `grep -n "status: \"PENDING_APPROVAL\"\|status: \"APPROVED\"\|status: \"CUSTOMS_CLEARED\"\|status: \"ISSUE\"" apps/backend/prisma/seed.ts` inside the `importOrders` array literal returns NO matches (all renamed to `logisticsStatus`)
    - `cd apps/backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "prisma/seed.ts" | wc -l` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "prisma/seed.ts" || echo "no seed.ts errors"</automated>
  </verify>
  <done>seed.ts compiles cleanly against the Phase 4 schema with no remaining approver/requiresApproval references and every import-order row using logisticsStatus with a valid pipeline-only value.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2 [BLOCKING]: Reseed the dev database</name>
  <read_first>
    apps/backend/package.json
  </read_first>
  <files>(none — data operation only)</files>
  <action>
Run from `apps/backend`: `npx prisma db seed`. This re-executes the updated `main()` in seed.ts,
which unconditionally wipes and recreates all business-demo data (stock transactions, sales/import
orders and items, customer licenses, inventory stock, customers, licenses, company, products,
suppliers, categories, dashboard KPIs) inside the existing `$transaction([...])` deleteMany block —
the System Admin user and RBAC seed are untouched (idempotent, upsert-keyed).

If the command fails with a TypeScript/runtime error, that indicates Task 1's edits are incomplete —
fix the reported field/line and re-run; do not proceed to later plans with a failed reseed.
  </action>
  <acceptance_criteria>
    - `cd apps/backend && npx prisma db seed` exits 0
    - After running, a quick check `npx prisma studio` is NOT required — instead verify via:
      `cd apps/backend && node -e "require('@prisma/client')"` exits 0 (client still loads), and the
      seed command's own console output contains no `Error`/stack trace
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx prisma db seed</automated>
  </verify>
  <done>Dev database reseeded successfully with Phase 4-shaped import/sales order data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed.ts execution -> dev database | Reseeding unconditionally wipes and recreates all business-demo tables; not a production concern (no production deployment exists per STATE.md) but still worth confirming `DATABASE_URL` before running |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | `npx prisma db seed` run against the wrong `DATABASE_URL` | accept | Same acceptance rationale as 04-01's clear step — this is a pre-v1 dev/demo project with no production database; seed.ts's own deleteMany block is the established, already-reviewed reset mechanism from every prior phase |
</threat_model>

<verification>
- `grep` checks in Task 1's acceptance_criteria pass
- `npx tsc --noEmit` shows zero errors originating from `prisma/seed.ts`
- `npx prisma db seed` completes successfully
</verification>

<success_criteria>
- seed.ts compiles and runs cleanly against the Phase 4 schema
- Dev database contains fresh demo data using logisticsStatus/status, with no approver/requiresApproval remnants
</success_criteria>

<output>
After completion, create `.planning/phases/04-approval-workflow/04-04-SUMMARY.md`
</output>
