---
phase: 04-approval-workflow
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - apps/backend/tests/order.noSelfApproval.test.ts
  - apps/backend/tests/salesOrder.creditLimit.test.ts
  - apps/backend/tests/salesOrder.discountLimit.test.ts
  - apps/backend/tests/audit.crud.orders.test.ts
  - apps/backend/tests/rbac.enforcement.writes.orders.test.ts
autonomous: true
requirements: [APPROVAL-02, APPROVAL-03, APPROVAL-04, APPROVAL-06]

must_haves:
  truths:
    - "No test in apps/backend/tests/ asserts on the removed requiresApproval field or on deliveryStatus/ImportOrder.status carrying an approval meaning"
    - "order.noSelfApproval.test.ts proves approvedById/approvedAt/rejectionReason are populated by the approve/reject endpoints"
    - "salesOrder.creditLimit.test.ts and salesOrder.discountLimit.test.ts prove status (not requiresApproval) drives the deferred-vs-immediate stock decrement"
  artifacts:
    - path: "apps/backend/tests/order.noSelfApproval.test.ts"
      provides: "Updated assertions for status/approvedById/approvedAt/rejectionReason"
      contains: "approvedById"
    - path: "apps/backend/tests/salesOrder.creditLimit.test.ts"
      provides: "status-based (not requiresApproval-based) assertions"
      contains: "status).toBe(\"PENDING_APPROVAL\")"
  key_links:
    - from: "apps/backend/tests/audit.crud.orders.test.ts"
      to: "POST /api/import-orders"
      via: "request body field renamed status -> logisticsStatus"
      pattern: "logisticsStatus"
    - from: "apps/backend/tests/rbac.enforcement.writes.orders.test.ts"
      to: "POST /api/import-orders"
      via: "request body field renamed status -> logisticsStatus"
      pattern: "logisticsStatus"
---

<objective>
Rewrite every existing test whose assertions or fixture payloads reference the fields this phase
renamed or removed (`requiresApproval`, `SalesOrder.approver`/`ImportOrder.approver`,
`deliveryStatus === "APPROVED"/"REJECTED"`, `ImportOrder.status` used as a client-submitted logistics
value). This is the single largest effort in the phase by file count — RESEARCH.md's Pitfall 1 and
Wave 0 Gaps both flag this explicitly as its own task, not an afterthought.

Purpose: without this sweep, `npm test` fails on assertions in files that look unrelated to this
phase's code changes, purely because of the field rename — exactly the failure mode RESEARCH.md
warns about. APPROVAL-02/03/04/06 are proven correct end-to-end only once these tests assert against
the real field names.
Output: Five updated test files, each still passing, covering the renamed/added fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-approval-workflow/04-CONTEXT.md
@.planning/phases/04-approval-workflow/04-RESEARCH.md
@.planning/phases/04-approval-workflow/04-03-SUMMARY.md
</context>

<interfaces>
Response shape from `POST /api/sales-orders/:id/approve` and `/reject` (after 04-03):
`{ ..., status: "APPROVED" | "REJECTED", approvedById: number, approvedAt: string, rejectionReason: string | null, deliveryStatus: <unchanged pipeline value> }`

Response shape from `POST /api/import-orders/:id/approve` and `/reject` (after 04-03):
`{ ..., status: "APPROVED" | "REJECTED", approvedById: number, approvedAt: string, rejectionReason: string | null, logisticsStatus: <unchanged pipeline value> }`

Request body for `POST /api/import-orders` (after 04-03): `logisticsStatus` replaces `status` as the
required client-facing field; `status` (if present) is now rejected with 400.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite order.noSelfApproval.test.ts assertions</name>
  <read_first>
    apps/backend/tests/order.noSelfApproval.test.ts
  </read_first>
  <files>apps/backend/tests/order.noSelfApproval.test.ts</files>
  <behavior>
    - Every existing `it(...)` block keeps its exact 3-step shape (create as creator -> self-approve/
      reject as creator (expect 403) -> approve/reject as a different holder (expect 200)) — only
      field-name assertions and fixture payloads change
    - New assertions added: `approvedById` matches the approver's numeric id, `approvedAt` is not
      null, and (on the reject test) `rejectionReason` matches the submitted reason
  </behavior>
  <action>
1. In the import-order creation payload (line ~133), change `status: "STAGING"` to
   `logisticsStatus: "STAGING"`.

2. Line 151 (`expect(approveRes.body.status).toBe("APPROVED");` for import) — leave as-is, already
   correct (import's approval field is still named `status`). Immediately after it, add:
   ```typescript
    expect(approveRes.body.approvedById).toBe(approver.id);
    expect(approveRes.body.approvedAt).not.toBeNull();
   ```
   (`approver.id` — confirm the exact property name returned by `createTestUserWithRoles`; if it
   returns `userId` instead of `id`, use that field name.)

3. Line 187 (`expect(approveRes.body.deliveryStatus).toBe("APPROVED");` for sales order approve) —
   this is exactly RESEARCH.md's Pitfall 1 bug: `deliveryStatus` stays `"PENDING"` per D-03; the new
   `status` field is what becomes `"APPROVED"`. Replace with:
   ```typescript
    expect(approveRes.body.status).toBe("APPROVED");
    expect(approveRes.body.approvedById).toBe(approver.id);
    expect(approveRes.body.approvedAt).not.toBeNull();
   ```

4. Line 223 (`expect(rejectRes.body.deliveryStatus).toBe("REJECTED");` for sales order reject) —
   same bug, replace with:
   ```typescript
    expect(rejectRes.body.status).toBe("REJECTED");
    expect(rejectRes.body.rejectionReason).toBe("not compliant");
   ```

5. Line 254 (`expect(approveRes.body.status).toBe("APPROVED");` for the null-createdById import
   order) — leave as-is (already correct), add the same `approvedById`/`approvedAt` assertions as
   step 2 immediately after it.

6. Line 299 (`expect(approveRes.body.deliveryStatus).toBe("APPROVED");` for the last-editor test) —
   same Pitfall 1 bug, replace with `expect(approveRes.body.status).toBe("APPROVED");`.

7. The direct-Prisma-insert fixture at line 232-246 (`prisma.importOrder.create({ data: { ...,
   status: "PENDING", ... } })`) sets the OLD overloaded field. Since this insert simulates
   "historical data with createdById: null" and does not go through the model layer, update it to
   the new schema shape: replace `status: "PENDING",` with `logisticsStatus: "STAGING",` (there is no
   `"PENDING"` value in either the new `LOGISTICS_STATUS_VALUES` or `ORDER_STATUS_VALUES` — `STAGING`
   is the correct logistics starting state, and `status` (approval) is left unset so it defaults to
   `APPROVED` per the schema default, which is fine since this test only exercises the approve
   action's permission/self-approval logic, not the threshold-routing logic).
  </action>
  <acceptance_criteria>
    - `grep -n "requiresApproval\|\.approver\b" apps/backend/tests/order.noSelfApproval.test.ts` returns NO matches
    - `grep -n "deliveryStatus).toBe(\"APPROVED\")\|deliveryStatus).toBe(\"REJECTED\")" apps/backend/tests/order.noSelfApproval.test.ts` returns NO matches
    - `grep -c "approvedById" apps/backend/tests/order.noSelfApproval.test.ts` returns at least 3
    - `npx vitest run tests/order.noSelfApproval.test.ts` (from `apps/backend`) passes all 5 test cases
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/order.noSelfApproval.test.ts</automated>
  </verify>
  <done>Every assertion in this file reflects the real status/approvedById/approvedAt/rejectionReason fields, and the full file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite salesOrder.creditLimit.test.ts and salesOrder.discountLimit.test.ts</name>
  <read_first>
    apps/backend/tests/salesOrder.creditLimit.test.ts
    apps/backend/tests/salesOrder.discountLimit.test.ts
  </read_first>
  <files>apps/backend/tests/salesOrder.creditLimit.test.ts, apps/backend/tests/salesOrder.discountLimit.test.ts</files>
  <behavior>
    - Same 3-case structure in both files (over-limit soft-block / approve-and-decrement / under-limit
      immediate-decrement) — only the field asserted changes from `requiresApproval` (boolean) to
      `status` (enum string)
    - The existing `InventoryStock.quantityOnHand`/`StockTransaction` count assertions before/after
      approve already directly verify APPROVAL-03 — keep them unchanged, just add `status` alongside
  </behavior>
  <action>
In `salesOrder.creditLimit.test.ts`:
- Line 120: `expect(res.body.requiresApproval).toBe(true);` -> `expect(res.body.status).toBe("PENDING_APPROVAL");`
- Line 159: `expect(res.body.requiresApproval).toBe(true);` -> `expect(res.body.status).toBe("PENDING_APPROVAL");`
- Line 169: `expect(approveRes.body.requiresApproval).toBe(false);` -> `expect(approveRes.body.status).toBe("APPROVED");`
- Line 207: `expect(res.body.requiresApproval).toBe(false);` -> `expect(res.body.status).toBe("APPROVED");`

In `salesOrder.discountLimit.test.ts` (grep-confirmed at lines 115, 155, 194 following the identical
pattern to `creditLimit.test.ts`):
- `expect(res.body.requiresApproval).toBe(true);` (over-discount cases) -> `expect(res.body.status).toBe("PENDING_APPROVAL");`
- `expect(res.body.requiresApproval).toBe(false);` (under-discount case) -> `expect(res.body.status).toBe("APPROVED");`

Do not change any other assertions (stock decrement checks, lot quantity checks, audit log checks) —
they already test the correct behavior structurally.
  </action>
  <acceptance_criteria>
    - `grep -n "requiresApproval" apps/backend/tests/salesOrder.creditLimit.test.ts apps/backend/tests/salesOrder.discountLimit.test.ts` returns NO matches
    - `grep -c "status).toBe(\"PENDING_APPROVAL\")\|status).toBe(\"APPROVED\")" apps/backend/tests/salesOrder.creditLimit.test.ts` returns at least 4
    - `npx vitest run tests/salesOrder.creditLimit.test.ts tests/salesOrder.discountLimit.test.ts` (from `apps/backend`) passes all cases
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/salesOrder.creditLimit.test.ts tests/salesOrder.discountLimit.test.ts</automated>
  </verify>
  <done>Both files assert on status instead of requiresApproval and pass in full.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix import-order create payloads in audit.crud.orders.test.ts and rbac.enforcement.writes.orders.test.ts</name>
  <read_first>
    apps/backend/tests/audit.crud.orders.test.ts
    apps/backend/tests/rbac.enforcement.writes.orders.test.ts
  </read_first>
  <files>apps/backend/tests/audit.crud.orders.test.ts, apps/backend/tests/rbac.enforcement.writes.orders.test.ts</files>
  <behavior>
    - Both files' import-order fixtures currently POST `{ ..., status: "RECEIVED" | "STAGING", ... }`
      — after 04-03, the create endpoint requires `logisticsStatus` instead and rejects any `status`
      key with 400
    - No assertion logic changes — only the request payload field name changes, since neither file
      asserts on the approval `status` value itself
  </behavior>
  <action>
In `audit.crud.orders.test.ts`, line 134: change `status: "RECEIVED",` to `logisticsStatus:
"RECEIVED",` in the `POST /api/import-orders` request body. This order is well under
`IMPORT_ORDER_APPROVAL_THRESHOLD` (its `totalValue` is `5 * 10 = 50`), so it will auto-`APPROVED` on
create and the existing assertion `expect(stockTx.length).toBeGreaterThan(0)` (checking the IN stock
transaction fired) continues to hold true under the dual RECEIVED+APPROVED gate from 04-02.

In `rbac.enforcement.writes.orders.test.ts`, line 140: change `status: "STAGING",` to
`logisticsStatus: "STAGING",` in the `POST /api/import-orders` request body used inside the
`it.each([...])` block. No other change needed — this test only exercises the approve endpoint's
permission gate, which is unaffected by the field rename.
  </action>
  <acceptance_criteria>
    - `grep -n "status: \"RECEIVED\"\|status: \"STAGING\"" apps/backend/tests/audit.crud.orders.test.ts apps/backend/tests/rbac.enforcement.writes.orders.test.ts` returns NO matches (inside the import-order POST body)
    - `grep -n "logisticsStatus: \"RECEIVED\"" apps/backend/tests/audit.crud.orders.test.ts` matches
    - `grep -n "logisticsStatus: \"STAGING\"" apps/backend/tests/rbac.enforcement.writes.orders.test.ts` matches
    - `npx vitest run tests/audit.crud.orders.test.ts tests/rbac.enforcement.writes.orders.test.ts` (from `apps/backend`) passes all cases
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx vitest run tests/audit.crud.orders.test.ts tests/rbac.enforcement.writes.orders.test.ts</automated>
  </verify>
  <done>Both files POST logisticsStatus instead of status for import-order creation and pass in full.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite -> production code correctness signal | A test that still passes but asserts the wrong field silently hides a regression — the primary risk this plan closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Repudiation | Stale test assertions masking a broken no-self-approval or field-rename regression | mitigate | Every assertion touching a renamed/removed field is rewritten in this plan against the real post-04-03 response shape, verified by each task's automated vitest run |
</threat_model>

<verification>
- `grep` checks in each task's acceptance_criteria pass
- `npx vitest run tests/order.noSelfApproval.test.ts tests/salesOrder.creditLimit.test.ts tests/salesOrder.discountLimit.test.ts tests/audit.crud.orders.test.ts tests/rbac.enforcement.writes.orders.test.ts` passes in full
</verification>

<success_criteria>
- Zero references to requiresApproval or approver remain anywhere under apps/backend/tests/
- All five updated test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-approval-workflow/04-05-SUMMARY.md`
</output>
