---
phase: 04-approval-workflow
plan: 07
type: execute
wave: 5
depends_on: ["04-05", "04-06"]
files_modified: []
autonomous: true
requirements: [APPROVAL-01, APPROVAL-02, APPROVAL-03, APPROVAL-04, APPROVAL-05, APPROVAL-06]

must_haves:
  truths:
    - "The full backend test suite passes with zero regressions"
    - "No source file under apps/backend/src or apps/backend/prisma references the removed requiresApproval or approver(String) fields"
    - "The ImportOrder RECEIVED-gated lot-creation path is grep-verifiably gated on BOTH logisticsStatus === RECEIVED AND status === APPROVED"
    - "No controller path allows a client-submitted status to bypass the approve/reject endpoints"
  artifacts: []
  key_links:
    - from: "apps/backend/src/models/importOrder.model.ts"
      to: "createOrUpdateLotsFromReceivingTx"
      via: "dual-condition gate"
      pattern: "logisticsStatus === \"RECEIVED\""
---

<objective>
Final phase-gate verification: run the full backend test suite, and grep-confirm the two
highest-risk regression points this phase explicitly flagged (the ImportOrder dual RECEIVED+APPROVED
gate, and the client-submitted-status bypass block) are actually present in the code, not just
believed to be present from earlier plans' acceptance criteria. This plan performs no new feature
work — it is a pure verification/regression gate, mirroring 03-10-PLAN.md's role in Phase 3.

Purpose: Building confidence that APPROVAL-01 through APPROVAL-06 all hold together as one coherent
system, not just as six independently-verified pieces — the full suite catches cross-file
interactions no single plan's scoped test run would.
Output: A passing full backend test suite and a documented pass/fail on each regression-risk grep
check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-approval-workflow/04-CONTEXT.md
@.planning/phases/04-approval-workflow/04-RESEARCH.md
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1 [BLOCKING]: Full backend test suite + regression grep sweep</name>
  <read_first>
    apps/backend/src/models/importOrder.model.ts
    apps/backend/src/controllers/salesOrder.controller.ts
    apps/backend/src/controllers/importOrder.controller.ts
  </read_first>
  <files>(none — verification only)</files>
  <action>
Run, in order, from `apps/backend`:

1. `npm test` (runs `vitest run` — the full suite, not a filtered subset). Every test file must pass,
   including ones this phase did not directly touch (e.g. license-block tests, inventory tests) —
   a failure there would indicate an unanticipated cross-file regression from this phase's field
   renames.

2. Regression grep checks (all must return the stated result):
   - `grep -n "logisticsStatus === \"RECEIVED\" && status === \"APPROVED\"" apps/backend/src/models/importOrder.model.ts`
     must match at least once in `create()`.
   - `grep -n "resolvedApprovalStatus === \"APPROVED\"" apps/backend/src/models/importOrder.model.ts`
     must match at least once in `update()`.
   - `grep -rn "requiresApproval" apps/backend/src apps/backend/prisma` must return NO matches
     anywhere in production source (test files were already swept in 04-05 — if any test file still
     matches, that is also a failure here).
   - `grep -rn "approver *String\|approver: string\|\.approver\b" apps/backend/src apps/backend/prisma`
     must return NO matches in production source.
   - `grep -n "status != null" apps/backend/src/controllers/salesOrder.controller.ts
     apps/backend/src/controllers/importOrder.controller.ts` must match in both files' create AND
     update actions (4 total matches), confirming client-submitted status is blocked on every
     create/update path, not just some.

3. If `npm test` fails, do NOT mark this task done — identify the failing file, determine whether it
   is a genuine regression (fix the underlying code from an earlier plan) or a missed sweep item (an
   overlooked test file referencing an old field — add it to the fix, following the same pattern
   already used in 04-05), and re-run until green.
  </action>
  <acceptance_criteria>
    - `npm test` (from `apps/backend`) exits 0 with zero failing tests
    - All 5 regression grep checks in step 2 return their stated result exactly
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npm test</automated>
  </verify>
  <done>Full backend suite is green and every regression-risk grep check confirms the dual-gate and client-bypass-block are actually present in the shipped code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Whole-system regression surface | This plan's only purpose is to verify no trust boundary established across 04-01 through 04-06 has silently regressed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | Client-submitted status bypass (cumulative check) | mitigate | Re-verified via grep across both controllers in this plan, closing out the threat first raised in 04-03 |
| T-04-02 | Tampering | ImportOrder RECEIVED+APPROVED dual gate (cumulative check) | mitigate | Re-verified via grep in this plan, closing out the threat first raised in 04-02 |
| T-04-03 | Repudiation / Elevation of Privilege | No-self-approval (cumulative check) | mitigate | Covered by the full suite's `tests/order.noSelfApproval.test.ts` and `tests/rbac.enforcement.writes.orders.test.ts` passing in step 1 |
</threat_model>

<verification>
- `npm test` exits 0 from `apps/backend`
- All 5 regression grep checks pass
</verification>

<success_criteria>
- APPROVAL-01 through APPROVAL-06 all hold under the full test suite
- The two highest-risk regression points (approval-bypass on receiving; client-submitted status
  bypass) are grep-confirmed absent from shipped code
</success_criteria>

<output>
After completion, create `.planning/phases/04-approval-workflow/04-07-SUMMARY.md`
</output>
