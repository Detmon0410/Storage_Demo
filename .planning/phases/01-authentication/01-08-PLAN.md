---
phase: 01-authentication
plan: 08
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - apps/backend/src/routes/category.routes.ts
  - apps/backend/src/routes/customer.routes.ts
  - apps/backend/src/routes/customerLicense.routes.ts
  - apps/backend/src/routes/dashboardKpi.routes.ts
  - apps/backend/src/routes/importOrder.routes.ts
  - apps/backend/src/routes/inventoryStock.routes.ts
  - apps/backend/src/routes/license.routes.ts
  - apps/backend/src/routes/product.routes.ts
  - apps/backend/src/routes/salesOrder.routes.ts
  - apps/backend/src/routes/stockTransaction.routes.ts
  - apps/backend/src/routes/supplier.routes.ts
  - apps/backend/tests/auth.enforcement.test.ts
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "Every POST and PUT endpoint (create/update, including customerLicense's renew action) across all 11 route files returns 401 when called with no valid Bearer access token"
    - "DELETE endpoints remain callable without a token at this stage (destructive enforcement is the final plan, 01-09)"
    - "A POST/PUT request WITH a valid access token still succeeds exactly as before"
  artifacts:
    - path: "apps/backend/tests/auth.enforcement.test.ts"
      provides: "Extended regression test: POST/PUT now assert 401-without-token"
      contains: "requireAuth"
  key_links:
    - from: "apps/backend/src/routes/category.routes.ts"
      to: "apps/backend/src/middleware/auth.ts"
      via: "requireAuth applied to .post()/.put() calls (in addition to .get(), applied in plan 01-07)"
      pattern: "requireAuth"
---

<objective>
D-08 stage 4: enforce authentication on every existing WRITE endpoint (POST create, PUT update, and `customerLicense`'s `POST /:id/renew` action) across all 11 route files. GET routes are already enforced (plan 01-07); DELETE remains open until the final plan.

Purpose: Continue closing AUTH-04 in the exact locked staging order — writes only after reads are already proven safe and the frontend already attaches tokens on every call (from plan 01-05's `client.ts`, so every existing page's create/update flows already send the `Authorization` header without any frontend change needed here).
Output: All POST/PUT routes in the 11 existing route files require `requireAuth`; DELETE routes remain open until plan 01-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-authentication/01-CONTEXT.md
@.planning/phases/01-authentication/01-RESEARCH.md
</context>

<interfaces>
<!-- State of apps/backend/src/routes/category.routes.ts AFTER plan 01-07 (GET already enforced) -->
```typescript
import { Router } from "express";
import {
  createCategory,
  deleteCategory,
  getCategory,
  listCategories,
  updateCategory,
} from "../controllers/category.controller.js";
import { requireAuth } from "../middleware/auth.js";

export const categoryRoutes = Router();

categoryRoutes.get("/", requireAuth, listCategories);
categoryRoutes.get("/:id", requireAuth, getCategory);
categoryRoutes.post("/", createCategory);
categoryRoutes.put("/:id", updateCategory);
categoryRoutes.delete("/:id", deleteCategory);
```

<!-- Target state after THIS plan -->
```typescript
categoryRoutes.get("/", requireAuth, listCategories);
categoryRoutes.get("/:id", requireAuth, getCategory);
categoryRoutes.post("/", requireAuth, createCategory);
categoryRoutes.put("/:id", requireAuth, updateCategory);
categoryRoutes.delete("/:id", deleteCategory);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply requireAuth to every POST/PUT route across all 11 route files</name>
  <files>apps/backend/src/routes/category.routes.ts, apps/backend/src/routes/customer.routes.ts, apps/backend/src/routes/customerLicense.routes.ts, apps/backend/src/routes/dashboardKpi.routes.ts, apps/backend/src/routes/importOrder.routes.ts, apps/backend/src/routes/inventoryStock.routes.ts, apps/backend/src/routes/license.routes.ts, apps/backend/src/routes/product.routes.ts, apps/backend/src/routes/salesOrder.routes.ts, apps/backend/src/routes/stockTransaction.routes.ts, apps/backend/src/routes/supplier.routes.ts</files>
  <read_first>apps/backend/src/routes/category.routes.ts, apps/backend/src/routes/customerLicense.routes.ts</read_first>
  <action>
`requireAuth` is already imported in all 11 files (from plan 01-07). Insert `requireAuth` as the middleware argument on every `.post(...)` and `.put(...)` call. Do NOT touch `.get(` (already done) or `.delete(` (next plan). Apply this identical transformation to each file:

**category.routes.ts** — `categoryRoutes.post("/", createCategory);` -> `categoryRoutes.post("/", requireAuth, createCategory);`; `categoryRoutes.put("/:id", updateCategory);` -> `categoryRoutes.put("/:id", requireAuth, updateCategory);`

**customer.routes.ts** — `customerRoutes.post("/", createCustomer);` -> `customerRoutes.post("/", requireAuth, createCustomer);`; `customerRoutes.put("/:id", updateCustomer);` -> `customerRoutes.put("/:id", requireAuth, updateCustomer);`

**customerLicense.routes.ts** — `customerLicenseRoutes.post("/", createCustomerLicense);` -> `customerLicenseRoutes.post("/", requireAuth, createCustomerLicense);`; `customerLicenseRoutes.put("/:id", updateCustomerLicense);` -> `customerLicenseRoutes.put("/:id", requireAuth, updateCustomerLicense);`; `customerLicenseRoutes.post("/:id/renew", renewCustomerLicense);` -> `customerLicenseRoutes.post("/:id/renew", requireAuth, renewCustomerLicense);` (this is a state-changing write action, must be enforced in this stage too)

**dashboardKpi.routes.ts** — `dashboardKpiRoutes.post("/", createDashboardKpi);` -> `dashboardKpiRoutes.post("/", requireAuth, createDashboardKpi);`; `dashboardKpiRoutes.put("/:id", updateDashboardKpi);` -> `dashboardKpiRoutes.put("/:id", requireAuth, updateDashboardKpi);`

**importOrder.routes.ts** — `importOrderRoutes.post("/", createImportOrder);` -> `importOrderRoutes.post("/", requireAuth, createImportOrder);`; `importOrderRoutes.put("/:id", updateImportOrder);` -> `importOrderRoutes.put("/:id", requireAuth, updateImportOrder);`

**inventoryStock.routes.ts** — `inventoryStockRoutes.post("/", createInventoryStock);` -> `inventoryStockRoutes.post("/", requireAuth, createInventoryStock);`; `inventoryStockRoutes.put("/:id", updateInventoryStock);` -> `inventoryStockRoutes.put("/:id", requireAuth, updateInventoryStock);`

**license.routes.ts** — `licenseRoutes.post("/", createLicense);` -> `licenseRoutes.post("/", requireAuth, createLicense);`; `licenseRoutes.put("/:id", updateLicense);` -> `licenseRoutes.put("/:id", requireAuth, updateLicense);`

**product.routes.ts** — `productRoutes.post("/", createProduct);` -> `productRoutes.post("/", requireAuth, createProduct);`; `productRoutes.put("/:id", updateProduct);` -> `productRoutes.put("/:id", requireAuth, updateProduct);`

**salesOrder.routes.ts** — `salesOrderRoutes.post("/", createSalesOrder);` -> `salesOrderRoutes.post("/", requireAuth, createSalesOrder);`; `salesOrderRoutes.put("/:id", updateSalesOrder);` -> `salesOrderRoutes.put("/:id", requireAuth, updateSalesOrder);`

**stockTransaction.routes.ts** — `stockTransactionRoutes.post("/", createStockTransaction);` -> `stockTransactionRoutes.post("/", requireAuth, createStockTransaction);` (this file has no PUT route — leave as-is)

**supplier.routes.ts** — `supplierRoutes.post("/", createSupplier);` -> `supplierRoutes.post("/", requireAuth, createSupplier);`; `supplierRoutes.put("/:id", updateSupplier);` -> `supplierRoutes.put("/:id", requireAuth, updateSupplier);`
  </action>
  <verify>
    <automated>cd apps/backend && grep -q 'post("/", requireAuth, createCategory)' src/routes/category.routes.ts && grep -q 'put("/:id", requireAuth, updateCategory)' src/routes/category.routes.ts && grep -q 'post("/:id/renew", requireAuth, renewCustomerLicense)' src/routes/customerLicense.routes.ts && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Every `.post(` and `.put(` call across all 11 route files (except `.delete(` calls) has `requireAuth` as its middleware argument
    - `customerLicenseRoutes.post("/:id/renew", ...)` specifically has `requireAuth` added
    - No `.delete(` call in any of the 11 files has `requireAuth` added yet (that is plan 01-09)
    - `stockTransaction.routes.ts` (which has no PUT route) only has its single POST updated, no PUT line added
  </acceptance_criteria>
  <done>All create/update (and customerLicense renew) endpoints across the 11 existing route files require a valid access token; DELETE routes remain open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend auth.enforcement.test.ts for write enforcement + full regression run</name>
  <files>apps/backend/tests/auth.enforcement.test.ts</files>
  <read_first>apps/backend/tests/auth.enforcement.test.ts</read_first>
  <behavior>
    - Test 5: `POST /api/categories` with no Authorization header -> now 401 (this replaces plan 01-07's "still succeeds" assertion — writes are now enforced).
    - Test 6: `PUT /api/categories/:id` with no Authorization header -> 401.
    - Test 7: `POST /api/categories` WITH a valid access token -> still succeeds (2xx), proving enforcement didn't break legitimate authenticated writes.
    - Test 8: `DELETE /api/categories/:id` with no Authorization header -> still succeeds (2xx-or-404, not 401) at THIS stage — destructive enforcement is plan 01-09, not yet applied.
  </behavior>
  <action>
Edit `apps/backend/tests/auth.enforcement.test.ts` (created in plan 01-07): update the existing "POST without token succeeds" assertion to now expect 401, and add Test 6/7/8 above. Reuse the existing test-user/login helper from plan 01-07's file — do not duplicate fixture setup. Clean up any category rows created via the authenticated POST test (Test 7) using `prisma.category.delete()` in an `afterAll`/`afterEach` hook, consistent with plan 01-07's cleanup convention.

Run the full backend suite:
```bash
pnpm --filter backend exec vitest run
```
  </action>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter backend exec vitest run` (full suite) exits 0
    - `auth.enforcement.test.ts` asserts 401 for unauthenticated POST and PUT on `/api/categories`
    - `auth.enforcement.test.ts` asserts a still-open (non-401) response for an unauthenticated DELETE at this stage
    - `auth.enforcement.test.ts` asserts an authenticated POST still succeeds (2xx)
  </acceptance_criteria>
  <done>Write-endpoint enforcement (AUTH-04, further progress) is verified by an automated regression test; full backend suite green; DELETE intentionally still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous client -> POST/PUT endpoints | Now requires proof of a valid session; DELETE remains open at this stage (closed in the final plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering | All 11 route files' POST/PUT endpoints | mitigate | `requireAuth` now gates every create/update of business data — an anonymous caller can no longer fabricate or modify products, orders, customers, licenses, stock records, etc. |
| T-01-19 | Tampering (destructive still open) | DELETE on all 11 route files | accept (transitional) | Deferred to plan 01-09 per the locked D-08 staged rollout — final, time-boxed intermediate state within this same phase |
</threat_model>

<verification>
1. `pnpm --filter backend exec vitest run` — full suite green, including extended enforcement assertions.
2. Manual: `curl -X POST http://localhost:4000/api/categories -d '{}' -H 'Content-Type: application/json'` (no auth header) returns 401; the same request with `-H 'Authorization: Bearer <valid token>'` returns 400 (validation error, since the body is empty) rather than 401 — proving the auth gate, not validation, is what changed.
</verification>

<success_criteria>
- Every POST/PUT endpoint (including customerLicense's renew action) across all 11 pre-existing route files requires authentication.
- DELETE remains intentionally open, per the locked staged-rollout order — final stage is the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-08-SUMMARY.md`
</output>
</content>
