---
phase: 01-authentication
plan: 09
type: execute
wave: 9
depends_on: ["01-08"]
files_modified:
  - apps/backend/src/routes/category.routes.ts
  - apps/backend/src/routes/customer.routes.ts
  - apps/backend/src/routes/customerLicense.routes.ts
  - apps/backend/src/routes/dashboardKpi.routes.ts
  - apps/backend/src/routes/importOrder.routes.ts
  - apps/backend/src/routes/inventoryStock.routes.ts
  - apps/backend/src/routes/license.routes.ts
  - apps/backend/src/routes/product.routes.ts
  - apps/backend/src/routes/salesOrder.routes.ts
  - apps/backend/src/routes/stockTransaction.routes.ts
  - apps/backend/src/routes/supplier.routes.ts
  - apps/backend/tests/auth.enforcement.test.ts
autonomous: false
requirements: [AUTH-04]

must_haves:
  truths:
    - "Every DELETE endpoint across all 11 route files returns 401 when called with no valid Bearer access token"
    - "Every existing API route (GET/POST/PUT/DELETE, all 11 route files) now requires a valid authenticated session — AUTH-04 fully satisfied"
    - "A logged-in user using the actual frontend UI can still perform every create/edit/delete action exactly as before this phase began"
  artifacts:
    - path: "apps/backend/tests/auth.enforcement.test.ts"
      provides: "Complete enforcement regression test: GET/POST/PUT/DELETE all assert 401-without-token, 2xx-with-token"
      contains: "requireAuth"
  key_links:
    - from: "apps/backend/src/routes/category.routes.ts"
      to: "apps/backend/src/middleware/auth.ts"
      via: "requireAuth applied to every method (get/post/put/delete) — full-file enforcement complete"
      pattern: "requireAuth"
---

<objective>
D-08 stage 5 (final): enforce authentication on every existing DELETE endpoint across all 11 route files. After this plan, every single existing API route in the application requires a valid authenticated session — AUTH-04 is fully satisfied, closing out this phase's core value statement ("no anonymous access to business data").

Purpose: This is the last of the three staged-enforcement plans (reads -> writes -> destructive), completing the rollout without ever having caused a full-app outage, per 01-RESEARCH.md Pitfall 1.
Output: Every route in every one of the 11 pre-existing route files requires `requireAuth`. Full end-to-end manual verification that the whole application still works correctly while fully authenticated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-authentication/01-CONTEXT.md
@.planning/phases/01-authentication/01-RESEARCH.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- State of apps/backend/src/routes/category.routes.ts AFTER plan 01-08 (GET, POST, PUT already enforced) -->
```typescript
categoryRoutes.get("/", requireAuth, listCategories);
categoryRoutes.get("/:id", requireAuth, getCategory);
categoryRoutes.post("/", requireAuth, createCategory);
categoryRoutes.put("/:id", requireAuth, updateCategory);
categoryRoutes.delete("/:id", deleteCategory);
```

<!-- Target FINAL state after THIS plan -->
```typescript
categoryRoutes.get("/", requireAuth, listCategories);
categoryRoutes.get("/:id", requireAuth, getCategory);
categoryRoutes.post("/", requireAuth, createCategory);
categoryRoutes.put("/:id", requireAuth, updateCategory);
categoryRoutes.delete("/:id", requireAuth, deleteCategory);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply requireAuth to every DELETE route across all 11 route files (final enforcement stage)</name>
  <files>apps/backend/src/routes/category.routes.ts, apps/backend/src/routes/customer.routes.ts, apps/backend/src/routes/customerLicense.routes.ts, apps/backend/src/routes/dashboardKpi.routes.ts, apps/backend/src/routes/importOrder.routes.ts, apps/backend/src/routes/inventoryStock.routes.ts, apps/backend/src/routes/license.routes.ts, apps/backend/src/routes/product.routes.ts, apps/backend/src/routes/salesOrder.routes.ts, apps/backend/src/routes/stockTransaction.routes.ts, apps/backend/src/routes/supplier.routes.ts</files>
  <read_first>apps/backend/src/routes/category.routes.ts</read_first>
  <action>
`requireAuth` is already imported in all 11 files (from plan 01-07). Insert `requireAuth` as the middleware argument on every remaining `.delete(...)` call — this is the last unenforced method in every file:

- `category.routes.ts`: `categoryRoutes.delete("/:id", deleteCategory);` -> `categoryRoutes.delete("/:id", requireAuth, deleteCategory);`
- `customer.routes.ts`: `customerRoutes.delete("/:id", deleteCustomer);` -> `customerRoutes.delete("/:id", requireAuth, deleteCustomer);`
- `customerLicense.routes.ts`: `customerLicenseRoutes.delete("/:id", deleteCustomerLicense);` -> `customerLicenseRoutes.delete("/:id", requireAuth, deleteCustomerLicense);`
- `dashboardKpi.routes.ts`: `dashboardKpiRoutes.delete("/:id", deleteDashboardKpi);` -> `dashboardKpiRoutes.delete("/:id", requireAuth, deleteDashboardKpi);`
- `importOrder.routes.ts`: `importOrderRoutes.delete("/:id", deleteImportOrder);` -> `importOrderRoutes.delete("/:id", requireAuth, deleteImportOrder);`
- `inventoryStock.routes.ts`: `inventoryStockRoutes.delete("/:id", deleteInventoryStock);` -> `inventoryStockRoutes.delete("/:id", requireAuth, deleteInventoryStock);`
- `license.routes.ts`: `licenseRoutes.delete("/:id", deleteLicense);` -> `licenseRoutes.delete("/:id", requireAuth, deleteLicense);`
- `product.routes.ts`: `productRoutes.delete("/:id", deleteProduct);` -> `productRoutes.delete("/:id", requireAuth, deleteProduct);`
- `salesOrder.routes.ts`: `salesOrderRoutes.delete("/:id", deleteSalesOrder);` -> `salesOrderRoutes.delete("/:id", requireAuth, deleteSalesOrder);`
- `stockTransaction.routes.ts`: `stockTransactionRoutes.delete("/:id", deleteStockTransaction);` -> `stockTransactionRoutes.delete("/:id", requireAuth, deleteStockTransaction);`
- `supplier.routes.ts`: `supplierRoutes.delete("/:id", deleteSupplier);` -> `supplierRoutes.delete("/:id", requireAuth, deleteSupplier);`

After this edit, every method on every route in all 11 files has `requireAuth` applied — verify by confirming no `.get(`, `.post(`, `.put(`, or `.delete(` call in any of the 11 files is missing `requireAuth` as its first middleware argument.
  </action>
  <verify>
    <automated>cd apps/backend && for f in category customer customerLicense dashboardKpi importOrder inventoryStock license product salesOrder stockTransaction supplier; do count_methods=$(grep -cE '\.(get|post|put|delete)\(' "src/routes/$f.routes.ts"); count_auth=$(grep -c "requireAuth" "src/routes/$f.routes.ts"); if [ "$count_auth" -lt "$count_methods" ]; then echo "$f.routes.ts: $count_auth requireAuth vs $count_methods route calls"; exit 1; fi; done; echo "all 11 files fully enforced"</automated>
  </verify>
  <acceptance_criteria>
    - Every `.get(`, `.post(`, `.put(`, `.delete(` call across all 11 route files now has `requireAuth` as a middleware argument (count of `requireAuth` occurrences per file >= count of route-registration calls, accounting for the one `import` line)
    - `apps/backend/src/routes/index.ts` remains unmodified throughout this entire 3-plan enforcement rollout (01-07/08/09) — no diff from its plan 01-04 state
    - `apps/backend/src/routes/auth.routes.ts` remains unmodified (login/refresh stay outside requireAuth; logout keeps its own inline requireAuth from plan 01-04)
  </acceptance_criteria>
  <done>Every method on every route across all 11 pre-existing route files requires a valid access token — AUTH-04 is fully satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete auth.enforcement.test.ts (DELETE enforcement) + full phase-wide regression suite</name>
  <files>apps/backend/tests/auth.enforcement.test.ts</files>
  <read_first>apps/backend/tests/auth.enforcement.test.ts</read_first>
  <behavior>
    - Test 9: `DELETE /api/categories/:id` with no Authorization header -> now 401 (replaces plan 01-08's "still succeeds" assertion).
    - Test 10: A full authenticated CRUD round-trip on `/api/categories` (create with token -> read with token -> update with token -> delete with token) all succeed, proving enforcement didn't break legitimate authenticated flows end-to-end.
  </behavior>
  <action>
Edit `apps/backend/tests/auth.enforcement.test.ts`: update the DELETE assertion from plan 01-08 (currently expects non-401) to now expect 401 when unauthenticated, and add Test 10 (full authenticated CRUD round-trip against `/api/categories`, cleaning up via the DELETE call itself as the final step so no test data lingers).

Run the ENTIRE backend test suite one final time — every test file from every plan in this phase (01-02 through 01-09) must be green together:
```bash
pnpm --filter backend exec vitest run
```

This is the phase-gate full-suite run required by 01-VALIDATION.md's Sampling Rate ("Before /gsd-verify-work: Full suite must be green").
  </action>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter backend exec vitest run` (full suite, every test file in the phase) exits 0
    - `auth.enforcement.test.ts` now asserts 401 for unauthenticated GET, POST, PUT, and DELETE (all 4 HTTP methods) against at least `/api/categories`
    - `auth.enforcement.test.ts`'s Test 10 authenticated round-trip (create/read/update/delete) succeeds end-to-end with a valid token
  </acceptance_criteria>
  <done>DELETE enforcement is verified; the full backend test suite (covering AUTH-01 through AUTH-07) is green as a single run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final phase verification — full application walkthrough with enforcement fully live</name>
  <files>apps/backend/src/routes/category.routes.ts, apps/backend/src/routes/customer.routes.ts, apps/backend/src/routes/customerLicense.routes.ts, apps/backend/src/routes/dashboardKpi.routes.ts, apps/backend/src/routes/importOrder.routes.ts, apps/backend/src/routes/inventoryStock.routes.ts, apps/backend/src/routes/license.routes.ts, apps/backend/src/routes/product.routes.ts, apps/backend/src/routes/salesOrder.routes.ts, apps/backend/src/routes/stockTransaction.routes.ts, apps/backend/src/routes/supplier.routes.ts</files>
  <what-built>
Authentication is now fully enforced across the entire backend: every existing API route (all 11 pre-existing route files, every HTTP method) requires a valid session. This is the final checkpoint for Phase 1 — confirms the whole staged rollout (login -> frontend wiring -> read enforcement -> write enforcement -> destructive enforcement) did not break any existing feature.
  </what-built>
  <action>
No code changes in this task — this is a manual verification checkpoint of the fully-enforced application (Tasks 1-2 of this plan, plus every prior plan in the phase). Follow the numbered steps in `<how-to-verify>` below against the running dev servers.
  </action>
  <how-to-verify>
1. Start both servers fresh: `pnpm dev` from the repo root.
2. Open `http://localhost:5173/` — confirm you're redirected to `/login` (if not already logged in from a prior session).
3. Log in as `admin` / `changeme123` (or your seeded credentials).
4. Visit each of the following pages and confirm data loads without error (no blank/broken states, no console errors about 401): Dashboard, Import Orders, Suppliers, Products, Categories, Inventory Lots, Stock Transactions, Licenses, Customers, Sales Orders.
5. On the Categories page, create a new category, edit it, then delete it — confirm all three actions succeed (proves POST/PUT/DELETE all work while authenticated).
6. Log out via the Topbar. Confirm you land back on `/login`.
7. With dev tools open (Network tab), try to manually replay one of the earlier authenticated requests (e.g. `GET /api/categories`) using `curl` with NO Authorization header: `curl -i http://localhost:4000/api/categories`. Confirm it returns `401`.
8. Confirm `pnpm --filter backend exec vitest run` (run this yourself or trust the automated verify from Task 2) shows all test files passing.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual browser + curl verification only, no automated check possible for a full visual application walkthrough; see acceptance_criteria for the pass/fail conditions a human confirms</automated>
  </verify>
  <acceptance_criteria>
    - All 8 steps in `<how-to-verify>` behave exactly as described, confirmed by the user
  </acceptance_criteria>
  <done>User has typed "approved" confirming all 8 manual verification steps passed; Phase 1 (Authentication) is complete.</done>
  <resume-signal>Type "approved" if all 8 steps pass, or describe which step failed and on which page/action</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous client -> any existing API route | Fully closed as of this plan — every route in the application (auth endpoints excluded by design) now requires a valid session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Elevation of Privilege / Repudiation | All 11 route files' DELETE endpoints | mitigate | `requireAuth` now gates every destructive action — an anonymous caller can no longer delete any business record (products, orders, customers, licenses, stock, etc.) |
| T-01-21 | Denial of Service (staged rollout leaving a gap) | Entire backend, across plans 01-07/08/09 | accept | The intermediate states (reads-only enforced, then +writes, then +deletes) were deliberate, time-boxed, and landed within the same phase execution — not a shipped or user-facing intermediate state; this checkpoint confirms the FINAL state has zero gaps |
</threat_model>

<verification>
1. `pnpm --filter backend exec vitest run` — full suite green (every test file across the entire phase).
2. Task 3 checkpoint (manual, blocking) — full application walkthrough confirms no regression, and AUTH-04 is demonstrably complete via the `curl` 401 check.
</verification>

<success_criteria>
- AUTH-04 fully satisfied: every existing API route requires a valid authenticated session.
- All of AUTH-01 through AUTH-07 are implemented, tested, and manually verified working together.
- No existing feature (CRUD on any of the 10 business entities) regressed as a result of this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-09-SUMMARY.md`
</output>
