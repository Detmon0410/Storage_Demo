---
phase: 02-rbac-audit-logging
plan: 14
type: execute
wave: 8
depends_on: ["02-13", "02-07"]
files_modified:
  - apps/frontend/src/api/types.ts
  - apps/frontend/src/api/resources.ts
  - apps/frontend/src/i18n/locales/en.ts
  - apps/frontend/src/i18n/locales/ja.ts
  - apps/frontend/src/pages/AuditLogPage.tsx
  - apps/frontend/src/App.tsx
autonomous: false
requirements: [AUDIT-04]

must_haves:
  truths:
    - "A SYSTEM_ADMIN or MANAGER_APPROVER can view the audit log, filtered by entity, user, action, and date range"
    - "Each row shows a View details action revealing formatted before/after JSON"
    - "The audit log table has no edit or delete affordance anywhere"
  artifacts:
    - path: "apps/frontend/src/pages/AuditLogPage.tsx"
      provides: "Read-only filterable audit log viewer per 02-UI-SPEC.md"
  key_links:
    - from: "apps/frontend/src/pages/AuditLogPage.tsx"
      to: "apps/frontend/src/api/resources.ts"
      via: "auditLogApi.list(filters)"
      pattern: "auditLogApi\\.list"
---

<objective>
Build the Audit Log Viewer per `02-UI-SPEC.md`: a read-only, filterable table (entity/user/action/
date-range) with a "View details" action revealing before/after JSON — the last piece of AUDIT-04.
Since `useResource` doesn't support server-side filter query params (confirmed during planning by
reading `hooks/useResource.ts` in full — every existing `list()` call takes zero arguments), this
page uses a bespoke fetch via the existing `request()` helper directly, not `useResource`.

Purpose: completes AUDIT-04's UI requirement, consuming plan 02-07's `GET /api/audit-logs` endpoint.
Output: `AuditLogPage.tsx` mounted at `/audit-logs`, gated by the nav item added in plan 02-12 — plus
a manual verification checkpoint per `02-VALIDATION.md`'s manual-only item for AUDIT-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rbac-audit-logging/02-UI-SPEC.md
@.planning/phases/02-rbac-audit-logging/02-VALIDATION.md
</context>

<interfaces>
<!-- Backend response shape from plan 02-07's listAuditLogs -->
```json
[
  { "auditLogId": 12, "entity": "Category", "entityId": "3", "action": "create", "userId": 1,
    "before": null, "after": { "categoryCode": "CAT-X", "categoryName": "X" },
    "createdAt": "2026-09-03T10:00:00.000Z", "user": { "id": 1, "username": "admin" } }
]
```
Query params accepted: `entity`, `userId`, `action`, `from` (ISO date), `to` (ISO date) — all optional.

<!-- request() helper signature, already read in full during planning (apps/frontend/src/api/client.ts) -->
```typescript
export async function request<T>(path: string, init?: RequestInit): Promise<T> {/* ...adds auth header, handles 401/errors... */}
```
Build query strings manually: `request<AuditLog[]>(\`/audit-logs?${new URLSearchParams(params).toString()}\`)`,
omitting empty/undefined filter values from `params` before constructing the `URLSearchParams`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AuditLog type, auditLogApi (bespoke filtered fetch), i18n keys</name>
  <files>apps/frontend/src/api/types.ts, apps/frontend/src/api/resources.ts, apps/frontend/src/i18n/locales/en.ts, apps/frontend/src/i18n/locales/ja.ts</files>
  <read_first>
    apps/frontend/src/api/types.ts
    apps/frontend/src/api/client.ts
  </read_first>
  <action>
    In `apps/frontend/src/api/types.ts`, append:
    ```typescript
    export interface AuditLog {
      auditLogId: number;
      entity: string;
      entityId: string;
      action: string;
      userId: number | null;
      before: unknown;
      after: unknown;
      createdAt: string;
      user?: { id: number; username: string } | null;
    }
    export interface AuditLogFilter {
      entity?: string;
      userId?: number;
      action?: string;
      from?: string;
      to?: string;
    }
    ```
    In `apps/frontend/src/api/resources.ts`, add:
    ```typescript
    import type { AuditLog, AuditLogFilter } from "./types";

    export const auditLogApi = {
      list: (filter: AuditLogFilter = {}) => {
        const params = new URLSearchParams();
        if (filter.entity) params.set("entity", filter.entity);
        if (filter.userId != null) params.set("userId", String(filter.userId));
        if (filter.action) params.set("action", filter.action);
        if (filter.from) params.set("from", filter.from);
        if (filter.to) params.set("to", filter.to);
        const qs = params.toString();
        return request<AuditLog[]>(`/audit-logs${qs ? `?${qs}` : ""}`);
      },
    };
    ```
    In both `apps/frontend/src/i18n/locales/en.ts` and `ja.ts`, add an `audit` namespace (mirroring
    existing namespace nesting) with these exact keys and English copy from `02-UI-SPEC.md`'s
    Copywriting Contract (translate to Japanese for `ja.ts`, matching that file's tone):
    `audit.title` = "Audit Log", `audit.subtitle` = "Review a complete history of create, update, delete, approval, and login activity across the system",
    `audit.emptyTitle` = "No audit events found", `audit.emptyDesc` = "Try widening your date range or clearing a filter.",
    `audit.filter.entity` = "Entity", `audit.filter.user` = "User", `audit.filter.action` = "Action", `audit.filter.dateRange` = "Date range",
    `audit.allEntities` = "All entities", `audit.allUsers` = "All users", `audit.allActions` = "All actions",
    `audit.viewDetails` = "View details", `audit.detailsBefore` = "Before", `audit.detailsAfter` = "After",
    `audit.action.create` = "Create", `audit.action.update` = "Update", `audit.action.delete` = "Delete",
    `audit.action.login` = "Login", `audit.action.logout` = "Logout", `audit.action.approve` = "Approve",
    `audit.action.reject` = "Reject", `audit.action.export` = "Export".
    Also add `nav.item.auditLogs` = "Audit Log" if not already present from plan 02-12/02-13.
  </action>
  <acceptance_criteria>
    - `grep -q "export interface AuditLog" apps/frontend/src/api/types.ts` succeeds
    - `grep -q "export const auditLogApi" apps/frontend/src/api/resources.ts` succeeds
    - `grep -q "audit.viewDetails" apps/frontend/src/i18n/locales/en.ts` succeeds
    - `grep -q "audit.viewDetails" apps/frontend/src/i18n/locales/ja.ts` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p .</automated>
  </verify>
  <done>Type-checks cleanly; both locale files have the full audit.* namespace</done>
</task>

<task type="auto">
  <name>Task 2: AuditLogPage.tsx — filter bar, read-only table, view-details modal</name>
  <files>apps/frontend/src/pages/AuditLogPage.tsx</files>
  <read_first>
    apps/frontend/src/pages/SuppliersPage.tsx
    apps/frontend/src/components/ui/Badge.tsx
  </read_first>
  <action>
    Create `apps/frontend/src/pages/AuditLogPage.tsx`. Local state: `rows`/`loading`/`error` (manual
    fetch via `auditLogApi.list`, re-fetched on filter change — no `useResource`, per this plan's
    objective), `filters` object (`entity`/`userId`/`action`/`from`/`to`), `viewing: AuditLog | null`
    for the details modal. Fetch on mount and whenever `filters` changes (a `useEffect` keyed on the
    filter values, debounced is not required for this data volume per `02-UI-SPEC.md`'s "default to
    live client-side... unless volume requires otherwise" note — client-refetch-on-change is
    acceptable here since the fetch already happens server-side per filter).

    - `PageHeader title={t("audit.title")} subtitle={t("audit.subtitle")}` with `filters` slot
      containing 4 controls: Entity `SelectField` (options: a hardcoded list of known entity name
      strings — `Category`, `Supplier`, `Product`, `Customer`, `CustomerLicense`, `License`,
      `ImportOrder`, `SalesOrder`, `InventoryStock`, `StockTransaction`, `User` — plus
      `t("audit.allEntities")` as the default/empty option), User `SelectField` (options: hardcoded
      or omit user-list fetching for this phase's scope — a free-text numeric input is acceptable if
      fetching the full user list here is out of scope; prefer a simple `TextInput type="number"`
      labeled `t("audit.filter.user")` with placeholder "User ID" if a full user-picker adds
      unwarranted scope), Action `SelectField` (options from the 8 known action types, each rendered
      via `t(\`audit.action.${value}\`)`, plus `t("audit.allActions")` default), Date range (two
      `TextInput type="date"` side by side, `from`/`to`). NO `actions` slot (per `02-UI-SPEC.md`,
      omit the prop entirely — this is a read-only viewer with no create action).
    - `DataTable` columns: Timestamp (`new Date(r.createdAt).toLocaleString()`), User (`r.user?.username ?? "-"`),
      Action (`Badge` with tone per `02-UI-SPEC.md`'s Color section: create/approve→success,
      update/export→info, delete→danger, login/logout→neutral, reject→warning — implement via a
      local `AUDIT_ACTION_TONE` lookup object, do not reuse `statusTone` from `lib/status.ts` since
      audit actions are a distinct tone mapping), Entity (`r.entity`), Entity ID (`r.entityId`),
      Details (`Button variant="ghost" size="sm" icon={<Eye/>} aria-label={t("audit.viewDetails")} onClick={() => setViewing(r)}`).
      No edit-pencil column, no delete affordance anywhere on this page (AUDIT-03: the UI must never
      suggest an audit entry is editable).
    - Sort: rows already arrive newest-first from the backend (`orderBy: { createdAt: "desc" }` in
      plan 02-07's model) — no client-side re-sort needed.
    - `viewing` modal: `Modal title={\`${viewing.entity} #${viewing.entityId}\`} onClose={() => setViewing(null)}`
      showing entity/action/user/timestamp as plain text, then two side-by-side `<pre className="font-mono text-xs">`
      blocks labeled `t("audit.detailsBefore")`/`t("audit.detailsAfter")` containing
      `JSON.stringify(viewing.before, null, 2)` / `JSON.stringify(viewing.after, null, 2)` (render
      `"null"` literal text when either is `null`, do not crash on it). No footer buttons other than
      the Modal's built-in close X (no Save/Cancel — this is a read-only viewer).
    - `LoadingState`/`ErrorState`/`EmptyState` (using `audit.emptyTitle`/`audit.emptyDesc`) exactly
      matching `SuppliersPage.tsx`'s conditional rendering shape.
  </action>
  <acceptance_criteria>
    - `grep -q "auditLogApi.list" apps/frontend/src/pages/AuditLogPage.tsx` succeeds
    - `grep -q "AUDIT_ACTION_TONE" apps/frontend/src/pages/AuditLogPage.tsx` succeeds
    - `grep -qv "Pencil\|Trash2" apps/frontend/src/pages/AuditLogPage.tsx` succeeds (confirms no edit/delete icons imported — AUDIT-03 UI guarantee)
    - `grep -q "JSON.stringify(viewing" apps/frontend/src/pages/AuditLogPage.tsx` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p .</automated>
  </verify>
  <done>AuditLogPage type-checks; filter bar + read-only table + details modal implemented with no edit/delete affordance</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mount route; manual verification of audit log filtering and detail view</name>
  <files>apps/frontend/src/App.tsx</files>
  <what-built>
    AuditLogPage.tsx mounted at /audit-logs, consuming the AUDIT-04 backend endpoint from plan 02-07.
  </what-built>
  <how-to-verify>
    1. Add `import { AuditLogPage } from "./pages/AuditLogPage";` to `apps/frontend/src/App.tsx` and
       a new route `<Route path="/audit-logs" element={<AuditLogPage />} />` inside the existing
       `<Route element={<AppShell />}>` block — do this as part of completing this task.
    2. Start both servers, log in as `admin`.
    3. Perform a few actions elsewhere in the app to generate audit entries: create a Category,
       update a Supplier, deactivate a user (from plan 02-13's page), log out and back in.
    4. Navigate to Audit Log (sidebar, Administration group). Confirm the new entries appear,
       newest-first.
    5. Filter by Entity = "Category" — confirm only the Category-related row(s) remain visible.
       Clear the filter, filter by Action = "Login" — confirm only login events remain. Filter by a
       date range that excludes today — confirm the list empties to the `audit.emptyTitle` state.
    6. Click "View details" on the Category create entry — confirm a modal opens showing `before: null`
       and `after` containing the created category's fields as formatted JSON.
    7. Confirm no edit or delete icon/button appears anywhere on this page.
    8. Log in as a non-admin, non-Manager user (e.g. a Sales Officer created in plan 02-13's
       verification) — confirm the Audit Log item is entirely absent from the sidebar, and confirm
       navigating directly to `/audit-logs` in the URL bar results in the page showing an error/empty
       state (since the underlying `GET /api/audit-logs` call returns 403) rather than any data.
  </how-to-verify>
  <action>
    No code changes in this task beyond the 2-line App.tsx route mount described in step 1 of
    how-to-verify below — this is primarily a manual verification checkpoint of Task 1-2's
    AuditLogPage implementation plus plan 02-07's backend, running against the live dev servers.
  </action>
  <verify>
    <automated>MISSING — manual browser verification only, no automated check possible for a full UI walkthrough including cross-role visibility checks; see acceptance_criteria for the pass/fail conditions a human confirms</automated>
  </verify>
  <acceptance_criteria>
    - All 8 steps in how-to-verify behave exactly as described, confirmed by the user
  </acceptance_criteria>
  <done>User has typed "approved" confirming all 8 manual verification steps passed, including filter correctness, the detail modal, and the non-admin visibility check</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| AuditLogPage.tsx -> /api/audit-logs | Pure presentation; the actual authorization boundary is the backend `requirePermission("AUDIT_LOG_VIEW")` from plan 02-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27b | Tampering | UI accidentally suggesting audit entries are editable | mitigate | No edit-pencil, delete, or any mutating affordance exists anywhere on this page — confirmed via acceptance criteria grep excluding `Pencil`/`Trash2` imports |
| T-02-28 | Information Disclosure | A non-admin/manager reaching `/audit-logs` directly via URL despite the hidden nav item | accept | Nav hiding is cosmetic only (per plan 02-12's threat model); the backend `requirePermission("AUDIT_LOG_VIEW")` independently denies the underlying API call regardless of whether the frontend route is reachable, so the page degrades to an error/empty state with no data ever transmitted to an unauthorized client |
</threat_model>

<verification>
- `npx tsc --noEmit -p apps/frontend` passes
- Manual checkpoint (Task 3) approved
</verification>

<success_criteria>
System Admin/Manager can view and filter the full audit history with a working before/after detail
view; the page has zero editing affordances; unauthorized users see no data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac-audit-logging/02-14-SUMMARY.md`
</output>
