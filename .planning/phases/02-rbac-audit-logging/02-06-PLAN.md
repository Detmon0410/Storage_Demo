---
phase: 02-rbac-audit-logging
plan: 06
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/backend/src/routes/product.routes.ts
  - apps/backend/src/routes/inventoryStock.routes.ts
  - apps/backend/src/routes/importOrder.routes.ts
  - apps/backend/src/routes/salesOrder.routes.ts
  - apps/backend/src/routes/stockTransaction.routes.ts
  - apps/backend/tests/rbac.enforcement.reads.batchB.test.ts
autonomous: true
requirements: [RBAC-04]

must_haves:
  truths:
    - "GET requests on product, inventoryStock, importOrder, salesOrder, and stockTransaction routes require the module's *_VIEW permission"
    - "SALES_OFFICER is denied SUPPLIER_VIEW-gated data but allowed on SALES_ORDER_VIEW; IMPORT_ORDER_VIEW excludes SALES_OFFICER per the matrix"
  artifacts:
    - path: "apps/backend/src/routes/importOrder.routes.ts"
      provides: "requirePermission(\"IMPORT_ORDER_VIEW\") on both GET routes"
  key_links:
    - from: "apps/backend/src/routes/salesOrder.routes.ts"
      to: "apps/backend/src/middleware/permission.ts"
      via: "requirePermission(\"SALES_ORDER_VIEW\")"
      pattern: "requirePermission\\(\"SALES_ORDER_VIEW\"\\)"
---

<objective>
Wire `requirePermission("<MODULE>_VIEW")` onto the GET routes of the remaining 5 route files:
`product`, `inventoryStock`, `importOrder`, `salesOrder`, `stockTransaction` — the second half of
the read-permission staged rollout (paired with plan 02-05, which handles the other 5 modules; no
file overlap between the two plans so both run in Wave 3 in parallel).

Purpose: same rationale as plan 02-05 — reads gated before writes, per RESEARCH.md Pattern 3's
staged rollout mandate.
Output: 5 route files with VIEW-permission enforcement on GET, one parameterized enforcement test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rbac-audit-logging/02-RESEARCH.md
</context>

<interfaces>
| Module | Route file | VIEW code | Roles with VIEW |
|---|---|---|---|
| Product | product.routes.ts | PRODUCT_VIEW | all 6 |
| InventoryStock | inventoryStock.routes.ts | INVENTORY_VIEW | all 6 |
| ImportOrder | importOrder.routes.ts | IMPORT_ORDER_VIEW | SYSTEM_ADMIN, MANAGER_APPROVER, IMPORT_COMPLIANCE_OFFICER, WAREHOUSE_DISTRIBUTION_OFFICER, FINANCE_ACCOUNTING_OFFICER (NOT SALES_OFFICER) |
| SalesOrder | salesOrder.routes.ts | SALES_ORDER_VIEW | all 6 |
| StockTransaction | stockTransaction.routes.ts | STOCK_TRANSACTION_VIEW | all 6 |

Wiring pattern identical to plan 02-05's `<interfaces>` block (mirrors `supplier.routes.ts`'s
`Router()`/`get("/")`/`get("/:id")` shape). `stockTransaction.routes.ts` has no `PUT /:id` route
(confirmed absent during planning discovery) — only `GET /`, `GET /:id`, `POST /`, `DELETE /:id`
exist; this plan only touches the two GET routes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire *_VIEW permission onto GET routes for product, inventoryStock, stockTransaction</name>
  <files>apps/backend/src/routes/product.routes.ts, apps/backend/src/routes/inventoryStock.routes.ts, apps/backend/src/routes/stockTransaction.routes.ts</files>
  <read_first>
    apps/backend/src/routes/product.routes.ts
    apps/backend/src/routes/inventoryStock.routes.ts
    apps/backend/src/routes/stockTransaction.routes.ts
  </read_first>
  <action>
    In each of the 3 files, add the `requirePermission` import and insert
    `requirePermission("PRODUCT_VIEW")` / `requirePermission("INVENTORY_VIEW")` /
    `requirePermission("STOCK_TRANSACTION_VIEW")` (respectively) immediately after `requireAuth` on
    that file's `GET /` and `GET /:id` routes only. Leave POST/PUT/DELETE routes with `requireAuth`
    only, unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "requirePermission(\"PRODUCT_VIEW\")" apps/backend/src/routes/product.routes.ts` returns `2`
    - `grep -c "requirePermission(\"INVENTORY_VIEW\")" apps/backend/src/routes/inventoryStock.routes.ts` returns `2`
    - `grep -c "requirePermission(\"STOCK_TRANSACTION_VIEW\")" apps/backend/src/routes/stockTransaction.routes.ts` returns `2`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p .</automated>
  </verify>
  <done>All 3 files type-check; GET routes gated, mutating routes untouched</done>
</task>

<task type="auto">
  <name>Task 2: Wire *_VIEW permission onto GET routes for importOrder, salesOrder; parameterized test</name>
  <files>apps/backend/src/routes/importOrder.routes.ts, apps/backend/src/routes/salesOrder.routes.ts, apps/backend/tests/rbac.enforcement.reads.batchB.test.ts</files>
  <read_first>
    apps/backend/src/routes/importOrder.routes.ts
    apps/backend/src/routes/salesOrder.routes.ts
  </read_first>
  <action>
    Apply the same wiring to `importOrder.routes.ts` (`IMPORT_ORDER_VIEW`) and `salesOrder.routes.ts`
    (`SALES_ORDER_VIEW`) as Task 1, GET routes only.

    Write `apps/backend/tests/rbac.enforcement.reads.batchB.test.ts` using `it.each`:
    ```typescript
    const cases = [
      { path: "/api/products", allowedRole: "SALES_OFFICER" },
      { path: "/api/inventory-stocks", allowedRole: "SALES_OFFICER" },
      { path: "/api/import-orders", allowedRole: "IMPORT_COMPLIANCE_OFFICER", deniedRole: "SALES_OFFICER" },
      { path: "/api/sales-orders", allowedRole: "SALES_OFFICER" },
      { path: "/api/stock-transactions", allowedRole: "WAREHOUSE_DISTRIBUTION_OFFICER" },
    ];
    ```
    Same assertion shape as plan 02-05's test: `allowedRole` gets non-403; where `deniedRole` is
    present (the `import-orders` row, since `SALES_OFFICER` lacks `IMPORT_ORDER_VIEW`), that role
    gets exactly 403.
  </action>
  <acceptance_criteria>
    - `grep -c "requirePermission(\"IMPORT_ORDER_VIEW\")" apps/backend/src/routes/importOrder.routes.ts` returns `2`
    - `grep -c "requirePermission(\"SALES_ORDER_VIEW\")" apps/backend/src/routes/salesOrder.routes.ts` returns `2`
    - `apps/backend/tests/rbac.enforcement.reads.batchB.test.ts` exists using `it.each`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run tests/rbac.enforcement.reads.batchB.test.ts</automated>
  </verify>
  <done>All 5 test cases pass, including the SALES_OFFICER-denied-on-import-orders case</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> GET routes (order/inventory data) | Import order data is commercially sensitive (pricing, supplier terms) and the matrix deliberately excludes Sales Officer from viewing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | Sales Officer reading Import Order pricing/supplier data outside their role scope | mitigate | `requirePermission("IMPORT_ORDER_VIEW")` excludes `SALES_OFFICER` per the seeded matrix; `rbac.enforcement.reads.batchB.test.ts` proves the denial |
</threat_model>

<verification>
- `pnpm exec vitest run tests/rbac.enforcement.reads.batchB.test.ts` passes
- `pnpm --filter backend test` (full suite) passes
</verification>

<success_criteria>
All GET routes across these 5 modules require the correct `*_VIEW` permission, verified by an
automated role-matrix test.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac-audit-logging/02-06-SUMMARY.md`
</output>
