---
phase: 02-rbac-audit-logging
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/backend/src/routes/customer.routes.ts
  - apps/backend/src/routes/customerLicense.routes.ts
  - apps/backend/src/routes/dashboardKpi.routes.ts
  - apps/backend/src/routes/license.routes.ts
  - apps/backend/src/routes/supplier.routes.ts
  - apps/backend/tests/rbac.enforcement.reads.batchA.test.ts
autonomous: true
requirements: [RBAC-04]

must_haves:
  truths:
    - "GET requests on customer, customerLicense, dashboardKpi, license, and supplier routes require the module's *_VIEW permission"
    - "A role lacking a module's VIEW permission is rejected with 403 on that module's GET routes"
  artifacts:
    - path: "apps/backend/src/routes/supplier.routes.ts"
      provides: "requirePermission(\"SUPPLIER_VIEW\") on both GET routes"
  key_links:
    - from: "apps/backend/src/routes/customer.routes.ts"
      to: "apps/backend/src/middleware/permission.ts"
      via: "requirePermission(\"CUSTOMER_VIEW\")"
      pattern: "requirePermission\\(\"CUSTOMER_VIEW\"\\)"
---

<objective>
Wire `requirePermission("<MODULE>_VIEW")` onto the two GET routes (`GET /` and `GET /:id`) of 5
existing route files: `customer`, `customerLicense`, `dashboardKpi`, `license`, `supplier`. This is
the "reads first" half of the staged rollout RESEARCH.md Pattern 3 mandates (mirroring Phase 1's
own reads-before-writes precedent) — POST/PUT/DELETE permission wiring + audit wrapping for these
same 5 modules happens later in plan 02-08, once this plan's routes are proven not to lock out any
seeded role.

Purpose: RBAC-04 requires every mutating endpoint to check permission, but reads must be gated too
(view-only roles still need `*_VIEW` to see the module) — proving read-gating first on 5 modules,
in parallel with plan 02-06's other 5, minimizes blast radius before write-gating lands.
Output: 5 route files with VIEW-permission enforcement on GET, one parameterized enforcement test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rbac-audit-logging/02-RESEARCH.md
</context>

<interfaces>
<!-- Exact permission codes per module, from plan 02-01's seeded matrix. VIEW-only wiring in this plan. -->

| Module | Route file | VIEW code | Roles with VIEW |
|---|---|---|---|
| Customer | customer.routes.ts | CUSTOMER_VIEW | all 6 |
| CustomerLicense | customerLicense.routes.ts | CUSTOMER_LICENSE_VIEW | all 6 |
| DashboardKpi | dashboardKpi.routes.ts | DASHBOARD_VIEW | all 6 |
| License | license.routes.ts | LICENSE_VIEW | all 6 |
| Supplier | supplier.routes.ts | SUPPLIER_VIEW | SYSTEM_ADMIN, MANAGER_APPROVER, IMPORT_COMPLIANCE_OFFICER, WAREHOUSE_DISTRIBUTION_OFFICER, FINANCE_ACCOUNTING_OFFICER (NOT SALES_OFFICER) |

Wiring pattern (from apps/backend/src/routes/supplier.routes.ts, already read in full during
planning — every one of the 5 files in this plan follows this exact `Router()`/`get("/")`/`get("/:id")`
shape, only the controller imports and permission code differ):
```typescript
import { requireAuth } from "../middleware/auth.js";
import { requirePermission } from "../middleware/permission.js";

xRoutes.get("/", requireAuth, requirePermission("X_VIEW"), listX);
xRoutes.get("/:id", requireAuth, requirePermission("X_VIEW"), getX);
```
`customerLicense.routes.ts` additionally has a `renewCustomerLicense` route
(`POST /:id/renew`) — do NOT touch it in this plan (it is a write action, deferred to plan 02-08).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire *_VIEW permission onto GET routes for customer, customerLicense, dashboardKpi</name>
  <files>apps/backend/src/routes/customer.routes.ts, apps/backend/src/routes/customerLicense.routes.ts, apps/backend/src/routes/dashboardKpi.routes.ts</files>
  <read_first>
    apps/backend/src/routes/customer.routes.ts
    apps/backend/src/routes/customerLicense.routes.ts
    apps/backend/src/routes/dashboardKpi.routes.ts
  </read_first>
  <action>
    In each of the 3 files, add the `requirePermission` import and insert
    `requirePermission("CUSTOMER_VIEW")` / `requirePermission("CUSTOMER_LICENSE_VIEW")` /
    `requirePermission("DASHBOARD_VIEW")` (respectively) immediately after `requireAuth` on that
    file's `GET /` and `GET /:id` routes only. Do not touch POST/PUT/DELETE/renew routes in these
    files — leave them with `requireAuth` only, unchanged, for now.
  </action>
  <acceptance_criteria>
    - `grep -c "requirePermission(\"CUSTOMER_VIEW\")" apps/backend/src/routes/customer.routes.ts` returns `2`
    - `grep -c "requirePermission(\"CUSTOMER_LICENSE_VIEW\")" apps/backend/src/routes/customerLicense.routes.ts` returns `2`
    - `grep -c "requirePermission(\"DASHBOARD_VIEW\")" apps/backend/src/routes/dashboardKpi.routes.ts` returns `2`
    - `grep -c "requirePermission" apps/backend/src/routes/customerLicense.routes.ts` returns `2` (renew route untouched)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && npx tsc --noEmit -p .</automated>
  </verify>
  <done>All 3 files type-check; GET routes gated, mutating routes untouched</done>
</task>

<task type="auto">
  <name>Task 2: Wire *_VIEW permission onto GET routes for license, supplier; parameterized test</name>
  <files>apps/backend/src/routes/license.routes.ts, apps/backend/src/routes/supplier.routes.ts, apps/backend/tests/rbac.enforcement.reads.batchA.test.ts</files>
  <read_first>
    apps/backend/src/routes/license.routes.ts
    apps/backend/src/routes/supplier.routes.ts
  </read_first>
  <action>
    Apply the same wiring to `license.routes.ts` (`LICENSE_VIEW`) and `supplier.routes.ts`
    (`SUPPLIER_VIEW`) as Task 1, GET routes only.

    Write `apps/backend/tests/rbac.enforcement.reads.batchA.test.ts` using `it.each` with this table
    (per RESEARCH.md Pitfall 6 — reuse `createTestUserWithRoles` from `tests/fixtures/testUser.ts`,
    built in plan 02-02):
    ```typescript
    const cases = [
      { path: "/api/customers", allowedRole: "SALES_OFFICER" },
      { path: "/api/customer-licenses", allowedRole: "IMPORT_COMPLIANCE_OFFICER" },
      { path: "/api/dashboard-kpis", allowedRole: "SALES_OFFICER" },
      { path: "/api/licenses", allowedRole: "IMPORT_COMPLIANCE_OFFICER" },
      { path: "/api/suppliers", allowedRole: "IMPORT_COMPLIANCE_OFFICER", deniedRole: "SALES_OFFICER" },
    ];
    ```
    For each row: a user with `allowedRole` gets a non-403 response on `GET <path>`; where
    `deniedRole` is present (only the `suppliers` row, since `SALES_OFFICER` lacks `SUPPLIER_VIEW`
    per the matrix), a user with `deniedRole` gets exactly 403 on `GET <path>`.
  </action>
  <acceptance_criteria>
    - `grep -c "requirePermission(\"LICENSE_VIEW\")" apps/backend/src/routes/license.routes.ts` returns `2`
    - `grep -c "requirePermission(\"SUPPLIER_VIEW\")" apps/backend/src/routes/supplier.routes.ts` returns `2`
    - `apps/backend/tests/rbac.enforcement.reads.batchA.test.ts` exists using `it.each`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run tests/rbac.enforcement.reads.batchA.test.ts</automated>
  </verify>
  <done>All 5 test cases pass, including the SALES_OFFICER-denied-on-suppliers case</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> GET routes | View-only access is still a boundary — e.g. Supplier data should not be visible to Sales Officers per the matrix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | A role viewing data the matrix does not grant it (e.g. Sales Officer reading Supplier data) | mitigate | `requirePermission("SUPPLIER_VIEW")` excludes `SALES_OFFICER` per plan 02-01's seed; `rbac.enforcement.reads.batchA.test.ts` directly proves the denial |
</threat_model>

<verification>
- `pnpm exec vitest run tests/rbac.enforcement.reads.batchA.test.ts` passes
- `pnpm --filter backend test` (full suite) passes
</verification>

<success_criteria>
All GET routes across these 5 modules require the correct `*_VIEW` permission, verified by an
automated role-matrix test.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac-audit-logging/02-05-SUMMARY.md`
</output>
