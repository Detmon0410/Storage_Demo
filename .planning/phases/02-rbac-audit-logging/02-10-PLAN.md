---
phase: 02-rbac-audit-logging
plan: 10
type: execute
wave: 4
depends_on: ["02-06", "02-04", "02-03"]
files_modified:
  - apps/backend/src/controllers/importOrder.controller.ts
  - apps/backend/src/controllers/salesOrder.controller.ts
  - apps/backend/src/routes/importOrder.routes.ts
  - apps/backend/src/routes/salesOrder.routes.ts
  - apps/backend/tests/audit.crud.orders.test.ts
  - apps/backend/tests/rbac.enforcement.writes.orders.test.ts
  - apps/backend/tests/order.noSelfApproval.test.ts
  - apps/backend/tests/order.genericUpdateRestriction.test.ts
autonomous: true
requirements: [RBAC-04, AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Create/update/delete on ImportOrder and SalesOrder each produce an atomic AuditLog row, using the tx-injected models from plan 02-04"
    - "POST /api/import-orders/:id/approve and /reject (and the sales-order equivalents) require IMPORT_ORDER_APPROVE / SALES_ORDER_APPROVE and emit an 'approve'/'reject' AuditLog row"
    - "The order's own creator (createdById) is rejected with 403 when attempting to approve or reject their own order"
    - "The generic PUT /:id endpoint can no longer set status to APPROVED or REJECTED — only the dedicated approve/reject endpoints can"
  artifacts:
    - path: "apps/backend/src/controllers/importOrder.controller.ts"
      provides: "approveImportOrder / rejectImportOrder with no-self-approval + audit"
    - path: "apps/backend/src/controllers/salesOrder.controller.ts"
      provides: "approveSalesOrder / rejectSalesOrder with no-self-approval + audit"
  key_links:
    - from: "apps/backend/src/routes/importOrder.routes.ts"
      to: "apps/backend/src/controllers/importOrder.controller.ts"
      via: "POST /:id/approve -> approveImportOrder (requirePermission(\"IMPORT_ORDER_APPROVE\"))"
      pattern: "\"/:id/approve\""
---

<objective>
Complete RBAC-04/AUDIT-01/AUDIT-02 coverage for the two order modules and implement D-01's minimal
approve/reject workflow ahead of Phase 4's full status machine: audit-wrap existing create/update/delete
using the tx-injected `ImportOrderModel`/`SalesOrderModel` from plan 02-04 (preserving their stock-side-effect
atomicity, same rationale as plan 02-09's StockTransaction handling); add dedicated
`POST /:id/approve` and `POST /:id/reject` endpoints for both entities, permission-gated and enforcing
no-self-approval (`createdById !== req.userId`) per D-01; and restrict the existing generic `PUT /:id`
endpoint from setting `status` to `"APPROVED"`/`"REJECTED"` directly, since RESEARCH.md Pitfall 3
confirmed zero existing test coverage depends on that behavior — closing the bypass path CONTEXT.md
flagged.

Purpose: this is the highest-stakes plan in the phase — orders are the entities the no-self-approval
control rule exists to protect (role doc §8.1, PROJECT.md's core value statement), and getting the
approve/reject + restriction sequencing wrong would either lock out legitimate approvals or leave the
bypass path CONTEXT.md warned about.
Output: audited order CRUD, 4 new approve/reject endpoints, a restricted generic update endpoint, 4
new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rbac-audit-logging/02-CONTEXT.md
@.planning/phases/02-rbac-audit-logging/02-RESEARCH.md
</context>

<interfaces>
| Module | CREATE code | EDIT code | DELETE code | APPROVE code | Roles with CREATE/EDIT |
|---|---|---|---|---|---|
| ImportOrder | IMPORT_ORDER_CREATE | IMPORT_ORDER_EDIT | IMPORT_ORDER_DELETE (Admin only) | IMPORT_ORDER_APPROVE (Admin, Manager) | SYSTEM_ADMIN, IMPORT_COMPLIANCE_OFFICER |
| SalesOrder | SALES_ORDER_CREATE | SALES_ORDER_EDIT | SALES_ORDER_DELETE (Admin only) | SALES_ORDER_APPROVE (Admin, Manager) | SYSTEM_ADMIN, SALES_OFFICER |

<!-- Approve endpoint reference shape, from 02-RESEARCH.md Code Examples (already codebase-conformant) -->
```typescript
export const approveImportOrder = asyncHandler(async (req: AuthenticatedRequest, res) => {
  const importOrderId = Number(req.params.id);
  const order = await prisma.$transaction(async (tx) => {
    const existing = await tx.importOrder.findUnique({ where: { importOrderId } });
    if (!existing) throw new HttpError(404, "Import order not found");
    if (existing.createdById != null && existing.createdById === req.userId) {
      throw new HttpError(403, "You cannot approve an order you created");
    }
    const updated = await tx.importOrder.update({ where: { importOrderId }, data: { status: "APPROVED", approver: req.username } });
    await AuditLogModel.record(tx, { entity: "ImportOrder", entityId: importOrderId, action: "approve", userId: req.userId ?? null, before: existing, after: updated });
    return updated;
  });
  res.json(order);
});
```
`req.username` is not currently set anywhere in `AuthenticatedRequest` (only `req.userId` is, per
`middleware/auth.ts`) — either look up the username via `RoleModel`/a direct `prisma.user.findUnique`
inside the transaction and use that, or simplify to storing `req.userId` in the `approver` field
(as a numeric fallback) with a comment noting the display-name resolution is cosmetic and can improve
in a later phase. Prefer the lookup: `const approverUser = await tx.user.findUnique({ where: { id: req.userId! }, select: { username: true } }); ... approver: approverUser?.username ?? null`.
`reject` is identical except `status: "REJECTED"` and `action: "reject"`; also accept an optional
`req.body.reason` string and if provided, include it in the audit `after` snapshot (there is no
dedicated `rejectionReason` column on `ImportOrder`/`SalesOrder` in this phase's schema — Phase 4 owns
adding one; capture it in the audit log's `after` JSON for now: `after: { ...updated, rejectionReason: req.body?.reason ?? null }`).
Apply the identical shape to `salesOrder.controller.ts`'s `approveSalesOrder`/`rejectSalesOrder`,
swapping `tx.importOrder` -> `tx.salesOrder`, `importOrderId` -> `salesOrderId`, and the permission
codes.

<!-- Generic update restriction — both controllers' existing updateImportOrder/updateSalesOrder -->
Reject (400) any `PUT /:id` request whose body includes `status: "APPROVED"` or `status: "REJECTED"`
BEFORE calling the model's update — these two values are now exclusively reachable via the dedicated
endpoints:
```typescript
if (status === "APPROVED" || status === "REJECTED") {
  throw new HttpError(400, "Use the dedicated approve/reject endpoint to change status to APPROVED or REJECTED");
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit-wrap create/update/delete for ImportOrder and SalesOrder (tx-injected models)</name>
  <files>apps/backend/src/controllers/importOrder.controller.ts, apps/backend/src/controllers/salesOrder.controller.ts, apps/backend/src/routes/importOrder.routes.ts, apps/backend/src/routes/salesOrder.routes.ts, apps/backend/tests/audit.crud.orders.test.ts</files>
  <read_first>
    apps/backend/src/controllers/importOrder.controller.ts
    apps/backend/src/controllers/salesOrder.controller.ts
    apps/backend/src/models/importOrder.model.ts
  </read_first>
  <action>
    Rewrite `createImportOrder`/`updateImportOrder`/`deleteImportOrder` to open
    `prisma.$transaction(async (tx) => {...})` and call `ImportOrderModel.create(data, tx)` /
    `.update(id, data, tx)` / `.delete(id, tx)` (plan 02-04's tx-injected signatures — do NOT call
    `tx.importOrder.create` directly, since the model's internal `createStockInTx` side effect must
    stay inside the same transaction), fetch `before` via `ImportOrderModel.findById` prior to
    update/delete, then `AuditLogModel.record(tx, {...})` with `entity: "ImportOrder"`. Pass
    `createdById: req.userId` into the `create` call's data (per plan 02-01's schema addition —
    this is what makes no-self-approval enforceable in Task 2). Apply the identical transformation to
    `salesOrder.controller.ts` with `SalesOrderModel`/`entity: "SalesOrder"`.

    Edit `importOrder.routes.ts`: `requirePermission("IMPORT_ORDER_CREATE")` on POST,
    `requirePermission("IMPORT_ORDER_EDIT")` on PUT, `requirePermission("IMPORT_ORDER_DELETE")` on
    DELETE. Edit `salesOrder.routes.ts` the same way with the `SALES_ORDER_*` codes. GET routes
    already wired in plan 02-06.

    Write `apps/backend/tests/audit.crud.orders.test.ts` asserting one create+update+delete
    round-trip on ImportOrder AND on SalesOrder each produce matching AuditLog rows, AND that
    creating an order still correctly posts its linked stock transaction (regression check on the
    `createStockInTx`/`createStockOutTx` side effect, reusing the assertion style from plan 02-04's
    `models.txClient.test.ts`).
  </action>
  <acceptance_criteria>
    - `grep -q "ImportOrderModel.create(" apps/backend/src/controllers/importOrder.controller.ts` succeeds
    - `grep -c "AuditLogModel.record(tx" apps/backend/src/controllers/importOrder.controller.ts` returns at least `3` (create/update/delete — approve/reject added in Task 2 will bring this higher)
    - `grep -q "createdById: req.userId" apps/backend/src/controllers/importOrder.controller.ts` succeeds
    - `grep -q "createdById: req.userId" apps/backend/src/controllers/salesOrder.controller.ts` succeeds
    - `grep -q "requirePermission(\"IMPORT_ORDER_DELETE\")" apps/backend/src/routes/importOrder.routes.ts` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run tests/audit.crud.orders.test.ts</automated>
  </verify>
  <done>Both order modules' CRUD is audited, permission-gated, and orders now carry createdById on creation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Approve/reject endpoints with no-self-approval enforcement</name>
  <files>apps/backend/src/controllers/importOrder.controller.ts, apps/backend/src/controllers/salesOrder.controller.ts, apps/backend/src/routes/importOrder.routes.ts, apps/backend/src/routes/salesOrder.routes.ts, apps/backend/tests/order.noSelfApproval.test.ts, apps/backend/tests/rbac.enforcement.writes.orders.test.ts</files>
  <read_first>
    apps/backend/src/controllers/importOrder.controller.ts
    apps/backend/src/controllers/salesOrder.controller.ts
  </read_first>
  <behavior>
    - Test 1: a user who created an ImportOrder (via `createImportOrder`, so `createdById` is set to
      them) attempting `POST /api/import-orders/:id/approve` on that same order gets 403
    - Test 2: a DIFFERENT user holding `IMPORT_ORDER_APPROVE` attempting the same approve on that
      order gets 200, and the order's `status` becomes `"APPROVED"`
    - Test 3: the same two behaviors hold for `SalesOrder`'s approve/reject endpoints
    - Test 4: an order with `createdById: null` (e.g. seeded historical data) can be approved by
      anyone holding `IMPORT_ORDER_APPROVE`/`SALES_ORDER_APPROVE` — a null creator never blocks
      approval (per plan 02-01's assumption A4)
  </behavior>
  <action>
    Add `approveImportOrder`/`rejectImportOrder` to `importOrder.controller.ts` and
    `approveSalesOrder`/`rejectSalesOrder` to `salesOrder.controller.ts` exactly per the
    `<interfaces>` template above. Wire routes:
    ```typescript
    importOrderRoutes.post("/:id/approve", requireAuth, requirePermission("IMPORT_ORDER_APPROVE"), approveImportOrder);
    importOrderRoutes.post("/:id/reject", requireAuth, requirePermission("IMPORT_ORDER_APPROVE"), rejectImportOrder);
    ```
    (reject uses the same `*_APPROVE` permission as approve — the role doc's matrix does not split
    approve/reject into separate codes; a user permitted to approve is permitted to reject the same
    order). Apply the identical route shape to `salesOrder.routes.ts` with `SALES_ORDER_APPROVE`.

    Write `apps/backend/tests/order.noSelfApproval.test.ts` implementing the 4 behaviors above (RED
    first — these will fail against the pre-Task-2 codebase, confirming the test actually exercises
    new behavior, then implement the endpoints above to turn them GREEN).

    Extend `apps/backend/tests/rbac.enforcement.writes.orders.test.ts` (new file) with `it.each`
    covering: a `SALES_OFFICER` denied on `POST /api/import-orders/:id/approve` (403, lacks
    `IMPORT_ORDER_APPROVE`), a `MANAGER_APPROVER` allowed (non-403, assuming they didn't create the
    order), and the equivalent pair for `SalesOrder`.
  </action>
  <acceptance_criteria>
    - `grep -c "createdById === req.userId" apps/backend/src/controllers/importOrder.controller.ts` returns at least `1`
    - `grep -c "createdById === req.userId" apps/backend/src/controllers/salesOrder.controller.ts` returns at least `1`
    - `grep -q "\"/:id/approve\"" apps/backend/src/routes/importOrder.routes.ts` succeeds
    - `grep -q "\"/:id/reject\"" apps/backend/src/routes/salesOrder.routes.ts` succeeds
    - `pnpm exec vitest run tests/order.noSelfApproval.test.ts tests/rbac.enforcement.writes.orders.test.ts` all pass
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run tests/order.noSelfApproval.test.ts tests/rbac.enforcement.writes.orders.test.ts</automated>
  </verify>
  <done>No-self-approval is enforced on both order types; approve/reject are permission-gated and audited</done>
</task>

<task type="auto">
  <name>Task 3: Restrict generic PUT from setting status to APPROVED/REJECTED</name>
  <files>apps/backend/src/controllers/importOrder.controller.ts, apps/backend/src/controllers/salesOrder.controller.ts, apps/backend/tests/order.genericUpdateRestriction.test.ts</files>
  <read_first>
    apps/backend/src/controllers/importOrder.controller.ts
    apps/backend/src/controllers/salesOrder.controller.ts
  </read_first>
  <action>
    In `updateImportOrder` and `updateSalesOrder`, add the guard from the `<interfaces>` block above
    at the top of the handler, before any model call: if `req.body.status` is `"APPROVED"` or
    `"REJECTED"`, throw `HttpError(400, "Use the dedicated approve/reject endpoint to change status to APPROVED or REJECTED")`.
    This closes the bypass path CONTEXT.md flagged — confirmed safe per RESEARCH.md Pitfall 3 (zero
    existing tests in this codebase exercise the generic update endpoint's `approver`/`status` write
    behavior for orders).

    Write `apps/backend/tests/order.genericUpdateRestriction.test.ts` asserting: `PUT /api/import-orders/:id`
    with `{ status: "APPROVED" }` in the body returns 400 with the guard's error message; the same
    for `SalesOrder`; and a `PUT` with any OTHER status value (e.g. `"STAGING"`) still succeeds
    normally (confirms the restriction is narrowly scoped to APPROVED/REJECTED only, not a blanket
    block on status edits).
  </action>
  <acceptance_criteria>
    - `grep -q "Use the dedicated approve/reject endpoint" apps/backend/src/controllers/importOrder.controller.ts` succeeds
    - `grep -q "Use the dedicated approve/reject endpoint" apps/backend/src/controllers/salesOrder.controller.ts` succeeds
    - `pnpm exec vitest run tests/order.genericUpdateRestriction.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && pnpm exec vitest run tests/order.genericUpdateRestriction.test.ts</automated>
  </verify>
  <done>Generic PUT can no longer set APPROVED/REJECTED status; all other status transitions via PUT remain unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> approve/reject endpoints | The single most sensitive control point in this phase — bypassing no-self-approval undermines the entire accountability model for orders |
| client -> generic PUT /:id | Previously a silent bypass path around approval controls; must be provably closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Repudiation / Tampering | Self-approval to bypass financial/compliance controls | mitigate | `existing.createdById === req.userId` check on both approve and reject for both entity types; proven by `order.noSelfApproval.test.ts` |
| T-02-21 | Elevation of Privilege | Generic `PUT /:id` used as a bypass path to set `status: "APPROVED"` without going through the permission-gated, no-self-approval-checked approve endpoint | mitigate | Explicit guard in `updateImportOrder`/`updateSalesOrder` rejects `status` values of `APPROVED`/`REJECTED` with 400, confirmed via `order.genericUpdateRestriction.test.ts`; verified safe against existing test coverage per RESEARCH.md Pitfall 3 |
| T-02-22 | Repudiation | `approver` field display-name resolution failing silently if `req.userId` has no matching user row | accept | `approverUser?.username ?? null` degrades gracefully to `null` rather than throwing; the `AuditLog` row (which always has `userId`) remains the authoritative accountability record regardless of the cosmetic `approver` string field |
</threat_model>

<verification>
- `pnpm exec vitest run tests/audit.crud.orders.test.ts tests/order.noSelfApproval.test.ts tests/rbac.enforcement.writes.orders.test.ts tests/order.genericUpdateRestriction.test.ts` all pass
- `pnpm --filter backend test` (full suite) passes
</verification>

<success_criteria>
Import and Sales Order CRUD is audited and permission-gated; dedicated approve/reject endpoints
enforce no-self-approval and emit correct audit events; the generic update endpoint can no longer be
used to bypass approval.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac-audit-logging/02-10-SUMMARY.md`
</output>
