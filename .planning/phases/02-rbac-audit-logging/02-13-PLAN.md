---
phase: 02-rbac-audit-logging
plan: 13
type: execute
wave: 7
depends_on: ["02-11", "02-12"]
files_modified:
  - apps/frontend/src/api/types.ts
  - apps/frontend/src/api/resources.ts
  - apps/frontend/src/i18n/locales/en.ts
  - apps/frontend/src/i18n/locales/ja.ts
  - apps/frontend/src/pages/UsersPage.tsx
  - apps/frontend/src/App.tsx
autonomous: false
requirements: [RBAC-05]

must_haves:
  truths:
    - "A SYSTEM_ADMIN can open the Users page, create a user with 2+ roles via checkboxes, and see the new user with role badges in the table"
    - "A SYSTEM_ADMIN can deactivate and reactivate a user from the table without a page reload"
    - "Attempting to remove the last SYSTEM_ADMIN's role or deactivate them surfaces the toastLastAdminGuard message, not a generic save-failed error"
  artifacts:
    - path: "apps/frontend/src/pages/UsersPage.tsx"
      provides: "Users list/create/edit/deactivate/reactivate screen per 02-UI-SPEC.md"
  key_links:
    - from: "apps/frontend/src/pages/UsersPage.tsx"
      to: "apps/frontend/src/api/resources.ts"
      via: "userApi.list/create/update/deactivate/reactivate/assignRoles"
      pattern: "userApi\\."
---

<objective>
Build the User Management screen per `02-UI-SPEC.md`: a list/create/edit/deactivate/reactivate page
reusing the existing CRUD table+modal pattern (`SuppliersPage.tsx`), with the phase's one genuinely
new interaction — a 6-role checkbox grid for multi-role assignment — as its own contained block
inside the existing `Modal`/`FormGrid` primitives (no new component library).

Purpose: this is the UI RBAC-05 requires — System Admin needs a way to actually perform the backend
operations plan 02-11 built.
Output: `UsersPage.tsx` wired to a new `userApi`, mounted at `/users`, gated by the nav item already
added in plan 02-12 — plus a manual verification checkpoint per `02-VALIDATION.md`'s manual-only
item for RBAC-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rbac-audit-logging/02-UI-SPEC.md
@.planning/phases/02-rbac-audit-logging/02-VALIDATION.md
</context>

<interfaces>
<!-- Backend response shape from plan 02-11 -->
```json
{ "id": 3, "username": "somchai", "status": "ACTIVE", "roles": ["SALES_OFFICER", "WAREHOUSE_DISTRIBUTION_OFFICER"] }
```
Backend endpoints (all under `/api/users`, all requiring SYSTEM_ADMIN — plan 02-11):
`GET /`, `GET /:id`, `POST /` (body: `{ username, password, status, roleCodes }`),
`PUT /:id` (body: `{ username, status }` — no password, no roleCodes), `POST /:id/deactivate`,
`POST /:id/reactivate`, `PUT /:id/roles` (body: `{ roleCodes }`).

<!-- The 6 roles, exact display labels per 02-UI-SPEC.md's Color section -->
```
SYSTEM_ADMIN -> "System Admin"
MANAGER_APPROVER -> "Manager / Approver"
IMPORT_COMPLIANCE_OFFICER -> "Import & Compliance Officer"
WAREHOUSE_DISTRIBUTION_OFFICER -> "Warehouse & Distribution Officer"
SALES_OFFICER -> "Sales Officer"
FINANCE_ACCOUNTING_OFFICER -> "Finance / Accounting Officer"
```
Badge tones (from `02-UI-SPEC.md` Color section, reuse existing 5-tone `Badge`):
`SYSTEM_ADMIN` -> danger, `MANAGER_APPROVER` -> warning, `IMPORT_COMPLIANCE_OFFICER` -> info,
`WAREHOUSE_DISTRIBUTION_OFFICER` -> neutral, `SALES_OFFICER` -> success,
`FINANCE_ACCOUNTING_OFFICER` -> info.

<!-- SuppliersPage.tsx full-file pattern (already read in full during planning) is the structural
template — reuse its imports list, useResource+local-state shape, Modal/ConfirmDialog/DataTable
composition. CheckboxField already exists in apps/frontend/src/components/ui/Field.tsx: -->
```typescript
export function CheckboxField({ label, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string }) {
  return (
    <label className="flex items-center gap-2 text-sm text-slate-700">
      <input type="checkbox" {...props} className="h-4 w-4 rounded border-slate-300 text-amber-600 focus:ring-amber-500" />
      {label}
    </label>
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: User type, userApi, i18n keys</name>
  <files>apps/frontend/src/api/types.ts, apps/frontend/src/api/resources.ts, apps/frontend/src/i18n/locales/en.ts, apps/frontend/src/i18n/locales/ja.ts</files>
  <read_first>
    apps/frontend/src/api/types.ts
    apps/frontend/src/api/resources.ts
    apps/frontend/src/i18n/locales/en.ts
  </read_first>
  <action>
    In `apps/frontend/src/api/types.ts`, append (matching the file's existing plain-interface style,
    no new imports needed):
    ```typescript
    export type RoleCode =
      | "SYSTEM_ADMIN"
      | "MANAGER_APPROVER"
      | "IMPORT_COMPLIANCE_OFFICER"
      | "WAREHOUSE_DISTRIBUTION_OFFICER"
      | "SALES_OFFICER"
      | "FINANCE_ACCOUNTING_OFFICER";

    export interface User {
      id: number;
      username: string;
      status: "ACTIVE" | "INACTIVE";
      roles: RoleCode[];
    }
    ```
    In `apps/frontend/src/api/resources.ts`, since `createResourceApi`'s generic shape doesn't cover
    `deactivate`/`reactivate`/role-assignment, hand-build `userApi` using the already-imported
    `request` helper (do not use `createResourceApi` for this one resource):
    ```typescript
    import type { User, RoleCode } from "./types";

    export const userApi = {
      list: () => request<User[]>("/users"),
      get: (id: number) => request<User>(`/users/${id}`),
      create: (body: { username: string; password: string; status: string; roleCodes: RoleCode[] }) =>
        request<User>("/users", { method: "POST", body: JSON.stringify(body) }),
      update: (id: number, body: { username: string; status: string }) =>
        request<User>(`/users/${id}`, { method: "PUT", body: JSON.stringify(body) }),
      deactivate: (id: number) => request<User>(`/users/${id}/deactivate`, { method: "POST" }),
      reactivate: (id: number) => request<User>(`/users/${id}/reactivate`, { method: "POST" }),
      assignRoles: (id: number, roleCodes: RoleCode[]) =>
        request<User>(`/users/${id}/roles`, { method: "PUT", body: JSON.stringify({ roleCodes }) }),
    };
    ```
    In both `apps/frontend/src/i18n/locales/en.ts` and `ja.ts`, add a `user` namespace object
    (mirroring the existing `supplier`/`product` namespace nesting depth and key style already in
    the file) with these exact keys and English copy (from `02-UI-SPEC.md`'s Copywriting Contract —
    for `ja.ts`, provide a reasonable Japanese translation of each, matching that file's existing
    tone/register for other namespaces):
    `user.add` = "Add user", `user.title` = "Users", `user.subtitle` = "Manage system users and their assigned roles",
    `user.emptyTitle` = "No users found", `user.emptyDesc` = "Try a different search term or add a new user.",
    `user.modalCreate` = "Add user", `user.modalEdit` = "Edit user", `user.modalSubtitle` = "Assign one or more roles. A user's access is the union of all assigned roles.",
    `user.modalCreateSubmit` = "Create user", `user.modalEditSubmit` = "Save changes",
    `user.field.username` = "Username", `user.field.password` = "Temporary password", `user.field.passwordHelp` = "The user should change this after first login.",
    `user.field.status` = "Status", `user.field.roles` = "Roles", `user.field.rolesHelp` = "Select at least one role.",
    `user.toastRolesRequired` = "Assign at least one role before saving.",
    `user.toast.created` = "{{username}} was added.", `user.toast.updated` = "{{username}} was updated.",
    `user.toast.deactivated` = "{{username}} was deactivated.", `user.toast.reactivated` = "{{username}} was reactivated.",
    `user.confirmDeactivateTitle` = "Deactivate {{username}}?", `user.confirmDeactivateMessage` = "{{username}} will lose access immediately on their next request. You can reactivate this account at any time.",
    `user.toastLastAdminGuard` = "At least one active System Admin must remain. Assign System Admin to another user first.",
    `user.role.SYSTEM_ADMIN` = "System Admin", `user.role.MANAGER_APPROVER` = "Manager / Approver",
    `user.role.IMPORT_COMPLIANCE_OFFICER` = "Import & Compliance Officer",
    `user.role.WAREHOUSE_DISTRIBUTION_OFFICER` = "Warehouse & Distribution Officer",
    `user.role.SALES_OFFICER` = "Sales Officer", `user.role.FINANCE_ACCOUNTING_OFFICER` = "Finance / Accounting Officer".
    Also add `nav.group.administration` = "Administration" and `nav.item.users` = "Users" if not
    already present (plan 02-12 added the `nav.ts` entries referencing these i18n keys — confirm
    they exist in both locale files; add them here if plan 02-12 didn't already, since `nav.ts`'s
    `labelKey`/`titleKey` strings will render as raw keys otherwise).
  </action>
  <acceptance_criteria>
    - `grep -q "export interface User" apps/frontend/src/api/types.ts` succeeds
    - `grep -q "export const userApi" apps/frontend/src/api/resources.ts` succeeds
    - `grep -q "toastLastAdminGuard" apps/frontend/src/i18n/locales/en.ts` succeeds
    - `grep -q "toastLastAdminGuard" apps/frontend/src/i18n/locales/ja.ts` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p .</automated>
  </verify>
  <done>Type-checks cleanly; both locale files have the full user.* namespace</done>
</task>

<task type="auto">
  <name>Task 2: UsersPage.tsx — list, multi-role create/edit modal, deactivate/reactivate</name>
  <files>apps/frontend/src/pages/UsersPage.tsx</files>
  <read_first>
    apps/frontend/src/pages/SuppliersPage.tsx
    apps/frontend/src/components/ui/Field.tsx
  </read_first>
  <action>
    Create `apps/frontend/src/pages/UsersPage.tsx` following `SuppliersPage.tsx`'s exact structural
    pattern (imports, `useResource`-less local fetch since role-assignment needs 2 sequential calls
    on edit — use local `rows`/`loading`/`error`/`saving` state managed manually via `userApi` calls
    rather than the generic `useResource` hook, since edit-mode saves must call BOTH
    `userApi.update(id, {...})` AND `userApi.assignRoles(id, roleCodes)` sequentially, which
    `useResource`'s single `update()` doesn't support):
    - `PageHeader` with `filters={<SearchInput .../>}` (search by username) and
      `actions={<Button variant="primary" icon={<Plus/>}>{t("user.add")}</Button>}`.
    - `DataTable` columns: Username (`font-semibold`), Roles (one `Badge` per role code, tone per the
      `<interfaces>` table above, `flex flex-wrap gap-1`), Status (`Badge tone={r.status === "ACTIVE" ? "success" : "neutral"}`),
      Actions (edit pencil `Button variant="ghost" size="sm"` with `aria-label={t("common.edit") + " " + r.username}`,
      and a deactivate/reactivate toggle icon button — `Ban`/`RotateCcw` from `lucide-react`, calling
      `userApi.deactivate`/`userApi.reactivate` directly on click, with `aria-label` stating the
      action per `02-UI-SPEC.md`'s icon-only labeling requirement — catch errors from this call with
      `toast.error`, checking specifically for the last-admin-guard message: if the caught error's
      message matches the backend's 409 text, show `t("user.toastLastAdminGuard")` instead of the
      raw backend message, otherwise show `t("common.saveFailed")`).
    - Create/Edit `Modal` at `width="max-w-lg"`, title `t(editing ? "user.modalEdit" : "user.modalCreate")`,
      subtitle `t("user.modalSubtitle")`. Fields: Username (`TextInput`, required), Temporary password
      (`TextInput type="password"`, required, ONLY rendered when `!editing` — omit entirely on edit
      per `02-UI-SPEC.md`'s explicit "no password field on edit" note), Status (`SelectField`
      Active/Inactive), then a Roles `Field`-less block spanning both columns
      (`<div className="sm:col-span-2">`) containing a `<span className="mb-1 block text-xs font-medium text-slate-600">{t("user.field.roles")}</span>`
      label plus a `grid grid-cols-1 gap-2 sm:grid-cols-2` of 6 `CheckboxField` instances (one per
      role code, label = `t(\`user.role.${code}\`)`, `checked`/`onChange` toggling that code in/out
      of local `form.roleCodes` array state) and the helper text `t("user.field.rolesHelp")` below.
    - On submit: if zero `roleCodes` selected, `toast.error(t("user.toastRolesRequired"))` and abort
      (do not call the API). If creating: `userApi.create({ username, password, status, roleCodes })`,
      then `toast.success(t("user.toast.created", { username }))`. If editing:
      `await userApi.update(editing.id, { username, status })` then
      `await userApi.assignRoles(editing.id, roleCodes)`, then
      `toast.success(t("user.toast.updated", { username }))`. Reload the list after either path
      (re-fetch `userApi.list()`, matching `useResource`'s own reload-after-mutate behavior).
    - Deactivate confirmation: reuse `ConfirmDialog` with `title={t("user.confirmDeactivateTitle", { username })}`,
      `message={t("user.confirmDeactivateMessage", { username })}` before calling `userApi.deactivate`.
      Reactivate has NO confirmation dialog (immediate action) per `02-UI-SPEC.md`.
    - Empty state: `EmptyState title={t("user.emptyTitle")} description={t("user.emptyDesc")}`.
  </action>
  <acceptance_criteria>
    - `grep -q "CheckboxField" apps/frontend/src/pages/UsersPage.tsx` succeeds
    - `grep -q "userApi.assignRoles" apps/frontend/src/pages/UsersPage.tsx` succeeds
    - `grep -q "toastRolesRequired" apps/frontend/src/pages/UsersPage.tsx` succeeds
    - `grep -q "aria-label" apps/frontend/src/pages/UsersPage.tsx` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit -p .</automated>
  </verify>
  <done>UsersPage type-checks; implements list/create/edit/deactivate/reactivate with the 6-role checkbox grid</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mount route; manual verification of user management + live role revocation</name>
  <files>apps/frontend/src/App.tsx</files>
  <what-built>
    UsersPage.tsx mounted at /users (SYSTEM_ADMIN-gated by nav, but the route itself should also be
    reachable directly for a SYSTEM_ADMIN — RequireAuth already covers authentication; authorization
    for the underlying API calls is enforced backend-side per plan 02-11, this route mount is purely
    presentational). Full user management flow: create user with 2 roles, deactivate, reactivate,
    self-lockout guard.
  </what-built>
  <how-to-verify>
    1. Add `import { UsersPage } from "./pages/UsersPage";` to `apps/frontend/src/App.tsx` and a new
       route `<Route path="/users" element={<UsersPage />} />` inside the existing `<Route element={<AppShell />}>`
       block, alongside the other authenticated routes — do this as part of completing this task
       (it's a 2-line mechanical addition, not part of the manual-verify step itself).
    2. Start both servers (`pnpm --filter backend dev`, `pnpm --filter frontend dev`), log in as
       `admin` / the seeded password.
    3. Navigate to Users (sidebar, Administration group). Click "Add user", fill in a username +
       temporary password, check 2 roles (e.g. Sales Officer + Warehouse & Distribution Officer),
       save. Confirm the new row appears with both role badges.
    4. Click the deactivate icon on that new user, confirm the dialog, confirm the row's Status badge
       becomes "Inactive". Click reactivate, confirm it returns to "Active" with no confirmation step.
    5. Edit the `admin` user (the only SYSTEM_ADMIN in a fresh seed) and uncheck "System Admin",
       save — confirm the `user.toastLastAdminGuard` message appears (not a generic save-failed
       toast) and the role is NOT actually removed (reload the page, confirm admin still has
       SYSTEM_ADMIN).
    6. Live revocation check (RBAC-06, per `02-VALIDATION.md`'s manual-only item): in a second
       browser/incognito window, log in as the test user created in step 3. In the admin window,
       remove one of that user's roles via the Edit modal. In the second window, perform any action
       gated by the removed role's permission (e.g. if Sales Officer was removed, try creating a
       Sales Order) WITHOUT logging out/in — confirm it is now denied (403), proving the permission
       change took effect on the very next request.
  </how-to-verify>
  <action>
    No code changes in this task beyond the 2-line App.tsx route mount described in step 1 of
    how-to-verify below — this is primarily a manual verification checkpoint of Task 1-2's
    UsersPage implementation plus plan 02-11's backend, running against the live dev servers.
  </action>
  <verify>
    <automated>MISSING — manual browser verification only, no automated check possible for a full UI walkthrough including a second-browser live-revocation check; see acceptance_criteria for the pass/fail conditions a human confirms</automated>
  </verify>
  <acceptance_criteria>
    - All 6 steps in how-to-verify behave exactly as described, confirmed by the user
  </acceptance_criteria>
  <done>User has typed "approved" confirming all 6 manual verification steps passed, including the last-admin guard and live role-revocation checks</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UsersPage.tsx -> /api/users | Pure presentation; the actual authorization boundary is the backend `requirePermission("USER_MANAGEMENT_FULL")` from plan 02-11, not this page's existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-26 | Elevation of Privilege | Relying on frontend route-mounting as an access boundary | accept | The `/users` route being reachable in the SPA router grants zero actual access — every `userApi.*` call still requires `USER_MANAGEMENT_FULL` server-side; a non-admin visiting `/users` directly would see only 403 errors on every list/action attempt |
</threat_model>

<verification>
- `npx tsc --noEmit -p apps/frontend` passes
- Manual checkpoint (Task 3) approved
</verification>

<success_criteria>
System Admin can fully operate user management from the UI; the last-admin guard surfaces its
specific message; live role revocation is confirmed end-to-end across two sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac-audit-logging/02-13-SUMMARY.md`
</output>
